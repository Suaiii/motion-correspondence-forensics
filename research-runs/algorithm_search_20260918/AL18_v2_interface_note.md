# AL18 同包接口处置：v1结果保留，v2仅修显式观测

planagent对冻结8ff3400作两项独立边界探针，指出N5显式hat_q的校验晚于unknown退出、
N2忽略显式hat_k。证据原样保存为AL18_interface_findings_source.json。
这两项不推翻147项固定代数结果，但说明v1没有完全实现协议所写的公共观测优先准则。
v1 bounds.py、输入、冻结与AL18_evidence.json全部原字节保留；原结果提交0546f63。

新bounds_v2.py依赖只读v1数学助手，不改其模块函数或文件：

1. N5在检查误差界前验证显式hat_q有限性，unknown B_D不能遮蔽NaN。
2. N2明确支持hat_k；先检查向量有限性/维度，再处理未知界；已知界时传给endpoint_result，
   其与端点计算的差必须受rho_K覆盖。不能重新计算0却让调用者以为提供的非零或NaN观测被检查。
3. 公开payload白名单拒绝未支持字段，N2 trace也有明确字段集合；不静默忽略未知输出别名。

定向回归仅两种模式及对应控制，每种h均保留，共24项：
N5 NaN+unknown、有限+unknown、有限+known；N2 NaN、有限零、有限+unknown L、
有界非零hat_k实际进入区间、未支持输出字段拒绝。
非零控制用已出现的1/64，rho_K=1/64，精确端点仍0，预期区间[0,1/32]，证明不是只做类型预检后继续忽略值。
没有新增科学误差族、随机搜索、模型或旧147/AL15重跑。

v2实现、24输入/精确预期与本说明先冻结并提交，然后才运行。
本执行者新增24次公开数值合同查询，mock R和端点readout均0。
planagent已执行的2次v1探针单列为外部审查记录，不混成自己的调用；本执行者不重复它们。
数学界仍来自原N1/N2/N4/N5，score与来源分离，所有修复只在有限回归范围接受。

原147项v1通过与v2的定向通过必须分别报告；没有声称v2已经重跑并验证原147项或任意真实AE。
