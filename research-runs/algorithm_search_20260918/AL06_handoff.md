# AL06 交接：经典投影下界有效，固定输入中发现两项遗漏

dispatch cc-al06-projected-certificates-20260919；task_id AL06。
researchagent_next；执行任务01a0b54a-3a46-72c0-9f2c-58bdb49c6424。
工作树 `D:/SUAI/codex/worktree/662f/脉冲神经网络`；分支 `codex/al06-projected-certificates`。
冻结提交1956f49，先保存源码/配置/全部输入/证明，再执行一次；结果提交见交接消息。
主DAG与主工作区、fallback、v2、AL01/AL04产物未改；未新建任务、未执行AL07。

## 判断

AL06执行完成，软件/有限对照检查通过，建议进入独立review。
L1粗化收缩和符号代表规则给出正确方向与1/3尺度的下界；数值检查未发现下界违反。
**本次受限平衡二分三角证书漏掉两项已冻结输入**，因此不能用L_bin=0替代原DJ的兼容性判断。
这仅仅是该经典约束子族的局限，不支持H-J相对于所有已有边际方法的新颖性，也不是检测器效果。
原参考DJ是本包的数值裁判，遗漏尚未有本执行者之外的独立复核；不能当作正式科学验收。

## 输入与结果

总24项含控制，不扩展搜索：18项基础均匀输入、4项重编号、2项非均匀域控制。
m4开发seed49979687；m8独立检查seed67867967。两组各12项，没有AL01已知二元分组提升。
每组4项显式一般Gamma见证、5项三边独立置换混合、2项重编号和1项域控制。
生成时没有运行任何候选或依据结果筛选。Gamma/生成置换仅供审计，全部方法仅使用相同Q。

22项有效均匀Q均调用原冻结H-J，另2项非均匀输入直接拒绝、无分数。
10项带兼容见证的记录（含两项重编号）DJ与L_bin均为0；12项参考DJ显著正（含两项重编号），其中2项L_bin为0。
九条记录存在显著下界差距，其中包含一个重编号重复及两项遗漏，不能当九个独立发现。

| 遗漏案例 | 原DJ | L_bin | 全投影最小面值 |
|---|---:|---:|---:|
| development_m4_08 | .009722222222222257 | 0 | .29166666666666674 |
| independent_check_m8_08 | .021527777777777878 | 0 | .15000000000000002 |

两项均来自事先固定的6个独立置换等权混合、.15均匀质量噪声，不是结果出来后调到的边界。
最小面值离负值还有明显裕量，未利用1e-8零阈值制造遗漏。
原Q完整保存在AL06_inputs.json，并原样提取到AL06_omissions.json；不做新拟合。

同Q简单量全部保留，无择边/择分数方向。例如两项遗漏的三向循环L1分别为
[.582013889,.308715278,.379548611]与[.736371528,.620824653,.768098958]；
三向JS分别为[.078418645,.015420739,.029305698]与[.109733274,.087903851,.115497848]。
这些摘要显然并非零，也没有与某个兼容输入完整匹配；因此不能声称H-J胜过全部简单基线。
三对熵和全部相关表在回执中，不仅仅保存最强投影。

## 已核对的证明与实现

对每个固定h，任意可行Gamma粗化后仍有二元均匀单点边际；逐格三角不等式保证每对L1不增。
把二元最优值与该粗化Gamma比较，再对原Gamma取min，得到D_h<=DJ；对h取max仍成立。
三个pair一直取平均，尺度为1/3。h变号引起边相关乘积符号d，其三坐标乘积为+1，
因此只是置换V的四个面，h[0]=+1的代表规则不漏不等式。

m4每变量3个代表，三变量27种；m8为35个代表、42,875种。先计算三个相关表，再广播四种面。
每项检查选中见证的8个符号组合及显式粗表/相关的边顺序；m4另核对全部6个带符号分区。
一致重编号的L_bin、DJ、所有循环/JS/熵量均通过。非均匀控制未误套均匀二元闭式。
原LP拟合Gamma重算目标在预定容差内，无接口/数值失败。本包已按24项停止，没有追加寻找遗漏。

## 与已有方法的关系

直接读取Sontag–Jaakkola 2007 [原论文](https://proceedings.neurips.cc/paper/2007/file/2b323d6eb28422cef49b266557dd31ad-Paper.pdf) §3–4。
AL06_design的逐公式表对应Definition 1、Theorem 1及式(4)—(7)：本包是平衡二分、单投影三角的受限经典特例。
未实现原切平面优化器、一般非平衡二分或多分区循环，不能将本包遗漏等同于原方法的完整投影约束也遗漏。
没有新增信息、真实视频效应或CCF-A成果声明。

## 版本、摘要与资源

主WORK_PLAN v1.4 SHA256：8e8fd95090ab5d02dd545badf1eb7fa722f67e3e277fca5f04cb15729c65ccb0。
主DAG、AL06工作单及AL01_REVIEW的实际读取摘要记录在AL06_freeze.json。
AL01-NUMPY-BOOL仍未修复；本包完全不调用其温度helper，不声称所有布尔类型已拒绝。

| 对象 | SHA256 |
|---|---|
| certificates.py | 159ef4b13102de794d382a5970990b2622e1c950565f21d4f120f57c33e4eced |
| run_package.py | a9cf727d91292e9130a76f528c323ec2e3e53223c60afcced7f0af04597b06be |
| config.json | 0c9109444f2e3aa0111e0f11a6186149b9e8ff7d3f64700122a3efe061f14a2b |
| AL06_inputs.json | 9cce6ead901e6fdbf30d53cb5e3566dc20edbc1cafadf1865899d0feaa1a4236 |
| AL06_evidence.json | 6499a1869a03dbe657b2f24aaf833e01125b1caea708162be22ae51cc8a79cdf |

原参考SHA256为82975f90f5040b6a781cd4cc559ee40b207d1e3df1f838edf49bf04a45887a8b；调用前后不变。
AL01/AL04及v2全文件摘要同样未变。完整清单见AL06_artifact_manifest.json。
CPU计算.4266134秒，BLAS实测2线程、HiGHS显式2线程；最大显式数组2,162,688字节（参考LP矩阵）。
m8单面数组343,000字节；进程峰值RSS未测。NumPy2.2.6、SciPy1.17.1。
无服务器、媒体、骨干、GPU、模型下载或训练，无遗留进程。累计资源实际消耗仍未知，未写成零。
单次小数组计时受分配/初始化影响；分区数指数增长，不构成256patch部署速度结论。

回放仅使用新输出，不覆盖本次结果：

```powershell
& E:/AAGenvid/.conda_envs/d3_cuda/python.exe research-runtime/algorithm_candidates/al06/run_package.py run --output <new-evidence.json>
```

## 下一项有新增信息的建议

下一独立包可只针对这两项已归档遗漏，比较一般非平衡二分三角与多分区循环，
辨别遗漏主要来自“只用平衡分区”还是“只用单投影三角”；仍明确作为经典控制。
非平衡二元边际不再均匀，不能机械套用本包“闭式等于二元最短距离”的说法；
若用循环违反量，应另证其L1下界尺度，必要时提供可复核的原问题对偶不可兼容证书。
这会给AL05真实的信息增量，且不必扩充随机搜索或假造检测标签。本包没有执行该后续或AL07。

正式macro增益>=.03、配对95%下界>0、至少4/5种子为正、来源/祖先/原生时间与最终集门槛均不变。
