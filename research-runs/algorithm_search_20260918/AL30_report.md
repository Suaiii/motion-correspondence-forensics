# AL30：按参数误差预算停止的独立软件修复

AL30单次冻结批次执行完成，`gate_result=pass`，范围仅为`accuracy_budgeted_optimizer_reference`。94项归档软件检查均通过，包括原三面板已知回归和唯一新增的delta=1/8尺度控制。AL27的`fail`永久保留，未更改其源码、输入、轨迹或报告。此次软件通过不改变科研假设与真实检测结论。

## 变更与预注册

冻结提交`1fa8bb8`在本批次首次数值目标求值前保存了全部实现、实际输入、配置、解析预期、协议及依赖/历史摘要。新目录`or1_head_accuracy_v2`中的objective.py与AL27文件字节相同；目标、梯度、Hessian、线性求解、Armijo、零初始化与最大200次迭代均未改变。唯一算法变化是传入统一推导的梯度L2停止阈值：

`tau_stop=min(1e-10, mu*epsilon_x/2)`，其中`mu=2*lambda_head`、`epsilon_x=1e-9`。

| 头 | 精确lambda | 精确mu | 精确停止阈值 | 实际float阈值 |
|---|---|---|---|---:|
| 候选，4参数 | 1/1000 | 1/500 | 1/10^12 | 1.0000000000000002e-12 |
| 自由，12参数 | 1/6000 | 1/3000 | 1/(6×10^12) | 1.6666666666666667e-13 |

实际float正则分别为0.001和0.00016666666666666666；mu分别为0.002和0.0003333333333333333。配置和每个头的结果均保存这些值以及未知的梯度舍入界`null`。参数最大绝对坐标差、目标绝对差的验收容差仍为1e-9，没有放宽。新规则不追加特例Newton步，只决定统一求解器何时自然停止。

精确算术中强凸性支持`||x-x*||_2<=||grad L(x)||_2/mu`。从浮点梯度推导严格参数保证，还需要独立控制梯度舍入误差；该误差界目前未知。本批次及所有标量根的`parameter_error_certified`/严格数值证书均为false。浮点梯度恰为0也不被视为数学上的零误差证明。

## 已知回归与唯一新增控制

原AL27输入按原文件字节SHA引用，其三面板行数据在新输入中逐项相同，明确标为`known_regression`。唯一新增面板为`full_rank_scale`：原始z与T、负侧z0=(1,1,0)重复4组、12对、均匀权重、lambda全部不变，正侧为z0+e_j/8。它仅仅是事先声明的优化尺度控制。

Fraction从实际冻结行数据验证四面板Delta-z/Delta-q秩依次为4/4、12/4、4/4、12/4，核对八个头的初始梯度及零性。288项逐组三元组Gram检查均满足A>=0、B>=0、AB-C²>=0；其中192项属于旧输入回归，96项属于新增尺度控制。逐组可行不代表同一真实视频和四滤波的联合可实现性。

以下目标均含原正则；所有梯度范数均为浮点计算值。标量参考采用独立stdlib公式、固定[0,64]括区、宽度1e-13、最多128次二分，未调用向量求解器。

| 面板及性质 | 候选目标 / 步数 | 自由目标 / 步数 | 自由终点梯度L2 | 自由参数与标量参考最大坐标差 |
|---|---:|---:|---:|---:|
| aligned，已知回归 | 0.11239309471543421 / 6 | 0.11239309471543424 / 6 | 8.6046e-17 | 2.3093e-14 |
| full_rank，已知回归 | 0.6931471805599453 / 0 | 0.25470089933726786 / 5 | 0（浮点） | 5.3291e-15 |
| symmetric，已知回归 | 0.6931471805599453 / 0 | 0.6931471805599453 / 0 | 0 | 零点由精确梯度及严格凸性核查 |
| full_rank_scale，新增控制 | 0.6931471805599453 / 0 | 0.4410763155957533 / 4 | 1.5023e-18 | 1.7764e-15 |

对齐回归的候选w各坐标为4.109879160372981，自由beta=(4.109879160372981,4.109879160372981,-8.219758320745962)重复4次；归档beta与Tw相等。候选标量坐标差1.1546e-14，候选梯度L2为2.0123e-16。

满秩回归的自由beta各坐标为7.794300533133798，标量参考7.794300533133793。AL27旧结果的相应坐标为7.794300501549412，旧误差3.158438133255004e-8仍保留为失败。只读比较归档确认：新运行的初始求值和前4步逐项等于旧优化轨迹；第4步梯度9.867353717817214e-11超过新的自由阈值，统一算法因此执行第5步后停止。此事前冻结修复没有事后手工补步。

新增delta=1/8控制的自由beta各坐标为8.229404687710113，标量参考8.229404687710115。原delta=1/4和新delta=1/8控制的固定beta=全1目标分别为0.5779394198788436、0.634599035317169，均低于log2；求解后目标进一步降低。它们核对同一人工目标下的参数包含关系，不能用于宣称实际来源上的自由基线优势或OR1科学否证。

候选在两个满秩控制中均为唯一零最优点；对称回归两种头均为唯一零最优点。空配对分别返回`no_training_support`和null参数，没有目标求值；非有限z分别在投影与求解前拒绝。有效的零最优模型与无支持/拒绝状态保持分离。

所有独立标量比较中，最大坐标差2.3093e-14，最大目标差5.5511e-17，均小于原1e-9容差。原始z下beta=Tw的四次目标恒等检查也通过。这些仅仅是已指定人工控制上的浮点数值一致性。

## 完整调用、冻结保全与限制

实际12次头输入尝试：8个有效头、2个空配对返回、2个非有限拒绝；10次求解入口。共21次线性求解、35次完整目标/梯度/Hessian求值：8次初始、21次线搜索试探、6次诊断（4个beta=Tw、2个固定全1）。三个标量参考各50次二分、53次导数求值，总计159次。没有失败或重跑；单批次回执阻止静默覆盖。

全部接受步长为1，没有触发回溯或线性失败分支，不声称这些分支得到数值覆盖。最大线性求解浮点残差8.6736e-19。完整参数、目标分项、梯度、Hessian、方向/RHS/残差、试探步长、Armijo界、停止原因、精确Fraction行与全部标量导数轨迹均保存在AL30_evidence.json；诊断求值不改变求解器终点ID。

NumPy2.2.6、Python3.11.15；各头顺序执行，原生OpenBLAS线程运行前后均为2。最大显式数组1152字节，所有Hessian至多12×12 float64，同样1152字节；每面板最多12对/24行/12维，满足单数组1MiB限制。未测峰值RSS。批次墙钟0.21701秒，进程CPU0.21875秒，包含导入和后验核查，不包括事前冻结哈希。

运行前后105项历史文件和5项选定直接依赖摘要相同，冻结输入与代码相同；105项包含AL27全部13项源/产物及其92项历史引用。主DAG/规划快照只读引用，不受本任务写入。旧AL27的失败及全部字节保持。未执行或等待AL31。

未调用probe/readout、模型/权重、媒体/真实数据、真实分类训练、Torch/SciPy Python、SSH/服务器/GPU或来源概率/AUROC。没有下载。NumPy附带的OpenBLAS库名含scipy不表示导入SciPy。历史累计花费和GPU时间仍未知，未填零。当前同一执行者的独立公式检查不替代不同复核者的科学审查；真实AE精度、数据/祖先、创新、预算与最终集门槛全部保持。
