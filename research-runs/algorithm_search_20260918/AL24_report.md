# AL24：精确小匹配与抽象一维目标的单次运行结果

包cc-al20-al24-baseline-matching-20260919；AL24；researchagent_next。
工作单SHA256 f4c00367c7a2ff4105ec86b969ac6a2570c270ec25c2380423171ec70fdbfa9f。
实现/全部实际输入/解析预期先以bef69a4冻结提交，再运行一次。
结果：四图、两类非法输入、M1/M2/非空零差分及空支持控制均符合预定合同，无未预期失败。

本包仅实现抽象软件参考：b已标准化且维12，q维4且仅首维非零；
没有估计真实均值/方差、祖先代表、宏层权重或拟合真实检测器。只获准求解三个预定的一维数学目标。

## 1. 精确匹配结果

以Fraction计算平方RMS费用和caliper，全部边与枚举解进入回执；不读取q或标签来打破平局。

| 图 | 可行部分匹配数 | 最终配对 | 基数 | 总平方RMS费用 | 最优平局数 |
|---|---:|---|---:|---:|---:|
| G1 | 5 | (p1,n2),(p2,n1) | 2 | 5/16 | 1 |
| G2 | 7 | (p1,n1),(p2,n2) | 2 | 0 | 1 |
| G3 | 7 | (p1,n1),(p2,n2) | 2 | 0 | 2 |
| G4 | 1 | 空 | 0 | 0 | 1（空解） |

G1验证基数优先于零费用单边；G2验证固定基数下费用优先；G3先保存两个完整最优平局，再选ID字典序最小者。
G4状态为no_training_support，而非训练完成；空解的费用0不表示存在已拟合的零权重模型。
重复ID与nonfinite b均被MatchingInputError明确拒绝，没有附加随机图或按结果扩例。

硬限制每侧<=4，但本批仅2×2；穷举参考不适用于真实大规模训练。
枚举复杂度随点数增长，不能用此小表的速度对完整匹配流水线作性能承诺。

## 2. 固定匹配目标

q正侧为(3,1)、负侧为(2,0)，仅第一坐标非零。G3实际选择M1，对应d=[1,1]；
另一预定最优平局M2对应d=[3,−1]，只用于数学反例，没有覆盖G3选择。
两者平均d同为1、匹配费用同为0，但目标不同。

    L(t)=mean_i log(1+exp(-t*d_i)) + .001*t²,
    w=(t,0,0,0).

实现稳定softplus、梯度与Hessian；其余三维固定0，不能称通用四维拟合。
所有结果使用相同原lambda=1e-3，未采用AL20的lambda/6基线去修改此任务。

| 控制 | w=1首坐标梯度（数值） | 求解首坐标t | 迭代数 | 最终计算梯度绝对值 |
|---|---:|---:|---:|---:|
| M1，d=[1,1] | −.2669414213699951 | 4.66512041240594 | 45 | 4.47e-15 |
| M2，d=[3,−1] | .2963904795486523 | .4530260127207839 | 45 | 9.51e-14 |
| 非空零差分 | .002 | 0（解析） | 0 | 0 |

M1在(1,10)，M2在(0,1)，符合事前由2<e<3及强凸性给出的粗范围。
非空零差分的唯一最优点为0，损失为log2+lambda*t²；这与G4空表的均值无定义不同。
空支持求解入口返回w=None、value=None，未求值任何目标/梯度，不输出“成功的w=0”。

## 3. 数值解释

共同初始括区[-16,16]，固定最多128次二分，宽度阈值1e-12；未根据结果扩括区或改配对/正则。
M1/M2均以浮点括区宽度约9.094947e-13停止。最终目标数值约.0311373886与.5870773492，
首坐标Hessian约.0112431874与.852566418；其余对角为.002。
全部迭代点/目标/梯度/Hessian在AL24_evidence.json保留，不仅仅输出converged。

普通binary64的exp、求和和梯度舍入没有严格误差上界；计算残差与括区只作为数值检查，
不借AL18取得参数误差或严格最优值证书。强凸性是目标的解析性质，不等于已实现一个有认证误差的优化器。
lambda的数学目标为1/1000，回执另记实际float与其hex，未隐藏数值表示。

唯一解/解存在仅适用于这些固定表，不证明其来自真实AE、包含来源信息或有域外检测增益。
没有输出来源概率、真实标签或分类性能；内部sigmoid仅用于计算导数。
同一确定性解重复若干次也不会产生独立种子证据，本包没有这样重复。

## 4. 调用、资源与未实现范围

6次匹配入口（4图+2错误），4次标量求解入口（M1/M2/zero/empty）。
值/梯度/Hessian联合求值共100次：M1与M2各49次（包括w=1），零差分2次，空支持0次。
模型/probe调用0；这不是100次训练批次或真实样本。

纯stdlib；匹配池完成后才运行目标池，各最多2个worker。
此次短运行分别观察到AL24match_0与AL24objective_0，未声称两个worker实际都被占用。
wall .151062秒、进程CPU .046875秒，不含解释器启动/最终JSON写出/报告编写。
最大描述子数值容器估计1,384字节，远低于1MiB；RSS未测。每侧实际2、硬上限4，b维12、q维4。

没有Torch/SciPy/NumPy、模型/权重/真实滤波/媒体/SSH/服务器/GPU或正式训练。
完整四维求解、标准化/祖先/代表选样、真实数据与正式机制评价仍未授权。
工作单、源码、输入、环境摘要及旧产物在执行前后校验；失败列表为空，原档案未改。

## 5. 下一判断

参考可用于验证后续实现是否遵守固定匹配与目标，但不能直接扩到正式数据训练。
先在未来预注册中锁定匹配ID、平局、层权重、正则度量和数值停止规则，再接入真实特征/祖先合同。
若后续需要通用四维或大规模匹配，必须另派、另冻结，并与本有限参考比对；本包不自动扩范围。
通过仅matching_and_objective_reference_software，AL05 fail与创新、资源、真实来源和最终集门槛保持。
