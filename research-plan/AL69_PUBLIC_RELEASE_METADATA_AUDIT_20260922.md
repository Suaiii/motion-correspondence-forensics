# AL69：公开基准发布字段与标签语义核验

planagent本职来源/协议审查。AL68现有服务器媒体核验和AL36不同执行者审查因已记录的提供方配置故障保留阻塞；不重复发送、不创建会话、不更换提供方。主DAG记本任务为当前本地包。

## 独立新增信息

AL63/64读的是论文管线，AL67仅盘点既有历史服务器清单。本包直接核查AIGVDBench作者公开发布是否已有可用的train/validation样本标识与来源/标签字段，区分可比黑盒评测与摄像来源/已知解码器机制归因。不能由论文或一个metadata缺字段推出所有公开数据不可用。

## 预定范围

以作者GitHub LongMa-2025/AIGVDBench及其链接的Hugging Face AIGVDBench/AIGVDBench为入口。每个仓库固定实际HEAD revision，记录请求时间、URL、响应/源码哈希和访问失败。只读公开README、数据卡、目录/API元数据；若存在可分离的train/validation清单，可各读取至多一份、每份上限8MiB，所有文本合计上限24MiB。按目录顺序选择，先读schema再决定是否能计算ID交集；不要抽取测试/闭源最终分区内容、视频、图片、权重或任何预测。目录中出现测试文件名只用于避免读取。

按真实字段统计行数、类型、缺失、显式标签/来源分布、完全相同文件ID的集合交集。caption仅审字段存在，不用其描述判断真实摄像或生成类别。文件ID前缀最多作为候选共同来源，不能推断未经核实的视觉祖先。不存在train/validation或超出上限时记录部分证据并结束，不递归扩大下载。

公开HTTP失败可进行一次普通同URL重试并保留两次回执，不改账户/认证绕过访问限制。解析问题保留失败日志，在结果中说明修订；它是数据接口故障，不自动记科学假设失败。仅运行获取/解析/哈希审计，无toy、拟合、训练、SSH/GPU或新媒体。

## 交付与裁决

交付reviews/AL69_PUBLIC_RELEASE_REVIEW_20260922.md和AL69_SOURCE_RECORD_20260922.json，附必要的紧凑可回放审计脚本。仅来源/schema审查通过不能释放模型机制或正式效能门槛；公开黑盒基准缺闭源权重哈希/seed/VAE不单独构成不可评测。数据卡的license与上游权利、非目标生成标签与摄像来源标签、官方文件划分与祖先独立性分别判断。

如存在可操作发布信息，形成有明确输入的后续取样/标签复核任务；如没有，记录精确缺口，不重复AL67泛化清单。AL68和AL36继续独立保留，下一轮本地工作从具体新证据设计，不只轮询错误。
