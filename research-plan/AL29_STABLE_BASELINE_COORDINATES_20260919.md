# AL29：避免消去优势的稳定紧凑基线合同

待执行planagent，须先完成AL25最终交付审查；与AL27多维优化参考独立。不修改AL25冻结实现或归档。问题来自AL25已观察、尚待最终验收的近抵消现象：数学上等价的A+B−2C在浮点下可能丢失直接差分能量，不能把这种差异当新信息。

限定推导与审查：

1. 在同一H、支持、分母下，用s=d_a+d_b、k=d_a−d_b定义r=(mean(Hs)^2/D, mean(Hk)^2/D, mean[(Hs)(Hk)]/D)。候选q就是r第二项；明确实现时复用候选已计算的HK/分母，使特征值直接共享，不能再次经三个大数相减获得它。
2. 对原z=(A/D,B/D,C/D)核对r=M z，其中M=[[1,1,2],[1,1,-2],[1,-1,0]]，写出逆矩阵及beta=M^T gamma。为保持AL20自由基线原有lambda/6度量，使用(lambda/6)||M^T gamma||²；不能换坐标后仍用任意同系数各向同性惩罚并叫同一个比较。
3. 核查gamma=(0,w,0)逐滤波嵌入时分数/数学惩罚包含候选。区分理想数学等价、literal q特征复用、任意浮点dot/优化器并不自动bitwise等价；这也不能消除probe/端点误差、overflow或真实误差未知。
4. 只引用最终通过审查的AL25近抵消回执，不重新运行或修改七个场，不增加来源标签。列未来公平比较所需记录：共享q、raw/稳定坐标、诱导正则、运算量、舍入差及覆盖。

输出reviews/AL29_STABLE_BASELINE_REVIEW_20260919.md及必要精确矩阵证据。验收仅 `stable_baseline_coordinate_contract`，不宣称新的算法原创或真实检测增益，不拟合、不读媒体、模型/服务器/GPU。
