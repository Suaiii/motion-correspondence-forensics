# AL22：公开探针配置与严格未见解码器的静态审查

planagent在AL18运行期间独立执行；不接管AL18或AL20。输入AL12未绑定的probe合同、AL17固定SDK源码及当前最终集候选计划。

仅审查官方CogVideoX与Wan各一个公开模型库的VAE配置及模型卡/许可/文件索引元数据，使用精确revision和哈希。优先小文本，合计预算不超过256KiB，不下载权重或媒体，不调用from_pretrained、Torch、服务器或GPU。必要的API元数据先限大小读取；超限则保留缺项，不能抓整个仓库替代。

要回答：与AL17默认类相比实际配置有哪些差异；posterior/latent维度/时间压缩和尺度绑定哪些已知、哪些仍未知；配置相同是否足以证明权重相同；若将这些候选作为公开probe，Cog/Wan生成器测试能否仍被称为严格未见decoder。

输出reviews/AL22_PUBLIC_PROBE_METADATA_REVIEW_20260919.md及紧凑来源清单/原始小文本。此任务仅形成候选配置证据和泄漏边界，不自动选定probe bank，不批准模型加载或改写最终集。实际权重哈希、SDK兼容、输出长度、精度、授权使用条件和资源若不能核实必须保留pending，不把同名/同config当相同权重证明。
