# AL52：既有有限h查询能否决定完整原次序差

planagent，独立于AL36和AL45。输入AL12原4次调用、固定h集合{1/4,1/8,1/16}及AL51分解；只做符号可辨识性审查，不执行模型/媒体、旧toy、随机搜索或标签实验。

科学问题：是否能从已有有限h输出推断C=R_a(R_b(u))-R_b(R_a(u))，从而省掉AL51提出的两次完整端点调用？不能仅因为K_h可观测就假定其分解项可观测。

固定同一输入u、r_a=R_a(u)、r_b=R_b(u)。列出原各h对R_a、R_b的全部查询点；在未查询的完整端点与这些有限查询点不重合、定义域为开邻域且输出有内部余量的条件下，检验是否可构造两个平滑映射版本在所有已查询输出及所需有限阶导数上相同，却给不同C。若构造成立，须证明原软路径和q/b/评分完全不变，并且J的变化按AL51代数补偿，不能仅给直觉。

明确碰撞/退化例外：r_b=u或其等于某个已知R_a查询点、另一分支相同等情形可能已有足够值。一般不识别结论须有条件，不能称任意输入绝对需要两次额外调用。已有可证明仿射结构、对称性或其他先验也是新增信息，应单列。

不选具体数字、h或核找有利例；仅用有限点隔离/支撑函数的解析构造。若使用光滑局部扰动，写出合法域、支撑和输出范围约束；不宣称实际AE类一定包含该构造。结论用于判断诊断额外信息是否必要，不是新算法或检测效果。

交付 AL52_FINITE_PROBE_IDENTIFIABILITY_REVIEW_20260919.md，包含查询表、严格构造/限制、下一最小观测与调用计数。真实协议仍需另行准入，无模型、训练、权重、媒体、服务器/GPU。
