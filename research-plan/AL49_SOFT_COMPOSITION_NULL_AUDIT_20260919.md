# AL49：原重建映射可交换是否传递到软混合路径

planagent，独立于AL36与AL45。输入AL12软步P_j^h=(1-h)id+hR_j、AL15线性/相同算子零例、AL47零对照前提。仅标准库符号与一个预先固定有理数核验，不使用旧审计数据或模型。

待检验假设：对一般非线性R_a、R_b，R_a∘R_b=R_b∘R_a是否足够保证P_a^h∘P_b^h=P_b^h∘P_a^h？不能把线性分配律套给非线性重建器。

固定标量域[0,1]，R_a(x)=x²、R_b(x)=x⁴=R_a∘R_a。原R互相可交换且保持域。软h固定为OR1主值1/8，仅核对u=1/2一项：精确算两个软路径端点、它们的差与K_h；h/频率/点均不搜索。结果若为0即保留零，不更换输入；若非零，仅否定上面一般充分性，不能赋予人工输入来源标签。

先写一般有限h差的表达，再说明同R、线性可交换R、h=0或h=1端点等各自保证什么；h=0的K/h²不定义，不能把端点相等当K存在。不要把一个非线性反例升格为真实AE事实或原创数学。

预先记录本工作单/脚本/固定有理输入摘要后一次计算；使用独立直接多项式展开或手工有理式交叉检查，不以回执passed字段代替计算。只写新的AL49证据，不改AL15、AL40失败或旧报告。交付 AL49_SOFT_COMPOSITION_NULL_REVIEW_20260919.md 与真实计算回执，明确这是改进null对照合法前提，不是OR1检测增益、算法准入或付费批次。
