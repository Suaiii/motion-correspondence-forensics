# AL46：匹配基线之后，实际 OR1 读出是否还有局部方向

planagent；输入 AL12 的 J_b delta≈0 与交互敏感假设、AL43 的实际 q 导数，以及 AL20/AL29 同信息强基线。独立于 AL36 的近邻/准入审查，也不等待 AL45 的统计来源恢复。仅符号线性代数设计，不增加旧合成场、随机例、真实媒体或模型调用。

科学问题：在共同支持内，即使 K 对 delta 有变化，q 也可能不变；进一步约束单步描述 b 不变后，是否存在任何候选真正可见的方向？设 B=J_b(u0)、Q=J_q(u0)，固定矩阵域与非零正分母。推导存在 delta 满足 B delta=0、Q delta!=0 的必要充分条件，要求同时给核空间、行空间和秩表达，不能只给名词。

只做符号推导：在普通欧氏度量下定义 B 的 Moore–Penrose 伪逆与零空间正交投影；给固定评分 w^Tq 的更严格条件。说明坐标/量纲变化、近零奇异值和估计误差对数值判秩的影响，不把经验小残差称为精确核空间。将 AL43 的 K=0 情形代入，说明一阶证书为何失效，不能据此否定二阶效果。

检验约束层次：原 b12 单步描述、扩充完整四调用基线 z/r、同信息已含 q 的强基线分别能否出现“额外信息”方向；不得把弱 b 有方向升格为超越含 q 的强基线。给真实机制实验需要估计的最小量及失败标准，当前不允许 JVP/反传/真实扰动或新增AE调用。

验收是条件推导和可否证设计清楚。该线性代数工具不是新算法，不创建新候选、不改 q/h/eta/匹配规则，不声称来源分布确有这样的方向或跨来源泛化。交付 AL46_MATCHED_DIRECTION_RANK_REVIEW_20260919.md，列未知的可微性、支持、Jacobian误差/数值秩和坐标度量。无真实数据、模型、分类器、服务器/GPU。
