# AL32：稳定坐标与正则集成限定验收

2026-09-19，planagent。核验冻结提交 `0685fbf` 和交付提交 `551b095`；AL32 按 `stable_readout_metric_integration_reference` 验收为 done/pass。研究任务随后因产品用量上限结束，不能因此丢弃已经提交的完整交接，也不能将其看作仍在运行。

12 项交付文件与结果 Git blob、7 项冻结源码/输入、118 项历史文件在两个工作区的摘要、5 项环境依赖及 5 项冻结时主工作区引用均核对一致。冻结提交早于单次 started 收据。已检查研究代码中的字面 q 复用、固定参数点、目标/梯度/Hessian、诱导度量及调用计数，没有运行研究端代码。

独立 Fraction 检查从 AL25 原归档选择的 3 场 × 4 滤波，包括正/负内积及近抵消。12 个 r_K 保持原 q 的 float.hex，三个共享四项评分逐位一致。负 C 与近抵消 raw compact=0、direct q>0 均保留；它们仅说明直接复用小量避免三项相消，不表示新增来源信息。

原 AL30 三个 known_regression 人工面板与输入逐项一致，没有使用新增 scale 面板。独立 60 位 Decimal 在同一 6 个固定点核对两种坐标的目标、梯度和 Hessian；共 12 组记录均满足原 1e-12 容差。矩阵双侧逆、参数映射、margin 与正则用 Fraction 核对；嵌入惩罚为 21/4000。见 [审查回执](AL32_ARCHIVE_REVIEW_20260919.json) 和 [审查脚本](audit_al32_archive_20260919.py)。

原 85 项软件检查通过。批次仅 12 次固定目标求值，没有优化、重新滤波、readout/probe/AE 调用。2 个原生线程，最大显式数组 1152B；RSS 未测。复核仅检查已有证据，不是新的真实数据实验或正式独立科学复核。

源码与结果以主提交 `6076d52`、`0f71154` 整合，DEVLOG 历史保留。AL05 与 AL27 的 fail 不变，AL30 的 pass 仅属精度参考；模型、数据、预算、最终集和创新门槛均未释放。下一步转向 AL34 单步统计的可辨识性审查，以及独立 AL35 最小真实机制判别协议草案，避免继续仅累加软件检查。
