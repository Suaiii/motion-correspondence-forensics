# AL27：保留软件精度失败，修正下一版本的停止合同

2026-09-19，planagent。执行已完成，整体 `gate_result=fail`。这次结果不否证OR1真实机制，也不能放行需要软件精度通过的后续任务。

核验12项交付/最终Git blob、冻结输入、92项历史文件和5项直接依赖。独立Fraction复核三个面板的秩、192项Gram条件及初始梯度；以60位Decimal核对六个归档终点的目标/梯度以及标量参考，未调用研究端优化器或追加Newton步。[复核脚本](audit_al27_failure_20260919.py)、[回执](AL27_FAILURE_REVIEW_20260919.json)。

唯一失败已确认：满秩自由头在梯度L2=9.867353717817214e-11处按冻结的1e-10阈值停止，但最大坐标差3.158438133255004e-8超过1e-9。69项中68项通过不能覆盖这一项失败；目标值近似相同也不能代替参数精度验收。

原因在于计划给出的停止阈值未保证达到更严格的坐标要求，研究端遵守了原停止规则。AL23已有强凸性关系：精确梯度下||x-x*||_2<=||grad L(x)||_2/mu；自由头mu=2/6000=1/3000，因此1e-10的梯度上限只给出3e-7的位置上界。两个原标准不是数学上不可同时满足，但前者不足以保证后者。仅增加最大迭代数也不会改变已经触发停止的行为。

下一版本另立AL30，保留所有候选/自由目标、原面板、原AL27失败及原求解轨迹，只按参数精度预算推导新的停止策略：

    epsilon_x=1e-9,
    tau_stop=min(1e-10, mu*epsilon_x/2).

候选阈值为1e-12，自由头为1/(6e12)。若还要严格证明位置精度，则必须另外证明梯度计算误差不超过另一半mu*epsilon_x/2；目前该误差界未知，因此新停止条件也只能作为数值算法策略，不能自称严格证书。原1e-9参数/目标验收不放宽，数据与lambda不变。

AL30只允许一次预注册修复批次，原三面板作为已知回归，另有一项事前声明的尺度变化软件检查；不能把回归通过说成新的科学样本或无限调参。新版本通过也不追改AL27的fail。

其他数学关系与限定输入检查保留：对齐子空间/惩罚关系、满秩候选零点、对称零点和空支持区分均符合所查控制。未触发的回溯与线性失败分支未获得数值覆盖。创新、真实AE误差、数据/祖先、预算与最终集门槛均保持。
