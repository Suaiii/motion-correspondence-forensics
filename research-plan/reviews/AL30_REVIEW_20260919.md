# AL30：精度预算修复通过，AL27失败保持

2026-09-19，planagent。审查1fa8bb8冻结与24933a6交付：AL30按 `accuracy_budgeted_optimizer_reference` 通过；AL27仍为done/fail，未修改任何旧输入、源码、结果或判断。

核验12项交付/最终Git blob、105项历史文件和5项依赖。新objective.py与AL27字节相同；只在新运行配置中传入按mu和参数目标推导的停止阈值。原三面板行数据完全相同并标为known_regression，唯一新增控制是full_rank偏移1/8。

独立Fraction核对四面板秩、288项Gram条件；用60位Decimal检查八个归档终点的目标/梯度，核对独立标量参考的原1e-9验收。94项原检查均通过。原满秩回归的初始及前4步求值与AL27完全相同，第4步残差超过新阈值，所以统一求解器自然执行第5步；没有手工续写旧轨迹。[复核脚本](audit_al30_archive_20260919.py)、[回执](AL30_ARCHIVE_REVIEW_20260919.json)。

原满秩回归坐标差降至5.33e-15，新增尺度控制约1.78e-15。它们只说明新策略在预定软件控制中达到了原精度要求，不是来源检测结果。实际float阈值与数学值分别记录，梯度舍入误差界仍unknown，因此不授予严格参数误差证书。

12次输入尝试、10次求解入口、21次线性求解、35次完整目标求值、159次标量导数求值完整保留。全部接受步长为1，仍没有回溯/线性失败分支的实测覆盖。本次复核没有运行优化器、模型或真实数据。

接续AL32稳定紧凑读出与正则坐标集成，避免以三项消去较差的路径设置弱基线；AL33独立审查理想投影重建下该响应能够表达什么。原AL05/AL27 fail及真实数据、模型、预算和最终集门槛均保持。
