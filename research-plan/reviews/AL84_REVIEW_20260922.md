# AL84 planagent审查：四调用到RGB归约桥接

审查对象为 `al84_trace_bridge_20260922` 的 v2 冻结源、输入、单次证据和交接；没有重跑第二批。`freeze.json` 绑定 bridge.py、reference.py、run_batch.py、config.json、input.json 及 AL79 reducer.py。输入哈希为 `03820573383fbfc462d45375ba1d3facbe65bc33b3a3247c31adf88f3090827a`，证据哈希为 `d124146a90f8c7dc4ed26aa8601a7d95269783be23635cb8c4629cb14fe0e55e`，交接哈希为 `1065395b09943ed8ecbd8aa272174f0293bfd659429633f50dab4df3db206e51`。

推导核对：四个角色固定为 `Ra_x/Rb_x/Ra_Pb/Rb_Pa`。由 AL15 已存核心，`Pa=(1-h)x+hRa(x)`、`Pb=(1-h)x+hRb(x)`；桥接返回 `v_a=Ra(x)-x`、`v_b=Rb(x)-x`，`d_a=(Ra(Pb)-Pb-Ra(x)+x)/h`、`d_b=(Rb(Pa)-Pa-Rb(x)+x)/h`，并逐元素核对 `d_a-d_b=(u_ab-u_ba)/h²`。独立算术参考逐项给出 d/v/K，5项均通过。之后仅调用一次只读 AL79 reducer，4个滤波各分2个时间块，返回 `rgb-reducer-v2`。

反例核对：错 sample、probe、stage、layout、shape、time slots、重复 role、缺失 role 共8项均被拒绝；第一次运行的路径错误保存为 `run_v0_startup_error.json`，没有把它静默改写为成功。源码静态检查通过，未见模型/媒体/服务器/GPU/网络调用。

替代解释与边界：通过结果只说明手工构造、已预计算的有限 RGB 张量可以被身份字段和算式一致地送入既有归约器；它不说明真实重建器输出、像素/时钟对应、probe 身份可获得、来源标签可用、检测器增益或新颖性。`d_a/d_b` 是对给定四个输出的代数重排，不能替代真实四调用。AL15/AL79的软件门范围、AL36反方准入、AL68数据就绪和正式效能门均保持原状态。
