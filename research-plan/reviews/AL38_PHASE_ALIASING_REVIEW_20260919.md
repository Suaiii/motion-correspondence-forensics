# AL38：stride 与相位混叠的限定数学审查

2026-09-19，planagent。该审查只固定离散采样/有限核模型，不运行模型、权重、媒体、服务器或 GPU。它服务于 H-P 设计边界，不产生来源检测收益或新算法准入。

对于纯采样 `S_s(x)_k=x_{sk}`，取 `x_t=exp(iωt)` 与 `z_t=exp(i(ω+2π/s)t)`，则 `S_s(z)=S_s(x)`。在没有额外幅相、位置、缓存或窗口观测时，两相位属于同一观测等价类，不能由 stride 单独辨识。

对于固定有限核 `A_{s,a}(x)_k=sum_r a_r x_{sk+r}`，输出由 `H_a(ν)=sum_r a_r exp(iνr)` 调制。`H_a(ω)=H_a(ω+2π/s)` 时仍完全混叠；两者不同只能说明该核提供了额外幅相信息，不能外推到任意 VAE、解码器或真实生成器。实际核、缓存/位置状态、边界和后处理必须先绑定。

结论仅是：架构 stride 不等于可见周期；纯采样可辨识性失败；有限核需要具体频响证据。未访问真实模型或数据，未形成检测器、AUROC、来源收益或正式创新主张。AL05、H-J/H-T 失败记录及预算/最终集门槛均保持。
