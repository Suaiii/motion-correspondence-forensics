# 研究执行回合 401 鉴权失败记录

2026-09-19 14:46（北京时间）检查科研任务 `01a0b54a-3a46-72c0-9f2c-58bdb49c6424`，回合 `01a0b833-341f-7bc0-81f6-05f617d6448c` 状态为 `failed`，工具快照标记任务 `systemError`。原始错误为：

`unexpected status 401 Unauthorized: Incorrect API key provided: agt_code******************************S19p. ... /v1/responses`

该回合没有助手产出、代码、数据、训练或其他执行证据。错误发生在研究执行层 API 鉴权，不能解释为项目 GPU 预算耗尽、服务器不可用或新机异常。既有 AL32 产物已在主工作区完成审查；不重发原研究包，不更换账户、模型、组织或访问程序来规避授权错误，不创建额外研究会话。

当前主 DAG 的 AL37 为 planagent 本地统计审查，继续执行；研究端 AL36 保持 `blocked/prepared_not_sent`，待外部研究会话真正恢复并提供状态变化后再重新评估。新 13340 服务器只读快照已独立记录，未启动付费作业。
