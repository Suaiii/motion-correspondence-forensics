# AL18：条件数值合同与定向接口处置验收

2026-09-19，planagent。审查researchagent_next的8ff3400、0546f63、4a771f7、1e823fd四个提交及完整交接。

**通过范围仅为固定v1数值算例和v2两类接口处置。** 原147项结果保留；两项真实接口缺口由新文件修复，没有将v1字节改成看似一直正确的版本。真实AE精度/误差、候选创新与付费阶段未放行。

## 核验

- 17项交付摘要、两次冻结的Git blob、54项保留文件及2项直接依赖摘要一致；冻结与结果提交的祖先关系成立。
- 阅读v1/v2和运行器，再从归档输入/trace以Fraction独立核对147项的界、状态、N1/N2放大系数、N2四调用账本、N4/N5能量与比值覆盖、重复有偏但误差未知的处理。没有重跑原147项候选查询或AL15。
- v1状态为24个条件区间、69个正能量未证、3个规定分母下界不正、12个未认证、39个非法输入；软件通过不等于所有输入都可认证。公式的前提必须来自有效误差声明，不能从这些mock外推真实Lipschitz常数。
- v2保存24项定向回归；独立再次执行原两种失效模式，两者均返回invalid_input。再用非零hat_k=1/64及rho_K=1/64核对实际返回1/64和区间[0,1/32]，确认显式观测被使用。

审查器和回执：[源码](audit_al18_archive_20260919.py)、[结果](AL18_ARCHIVE_REVIEW_20260919.json)。

## 计数和限制

researchagent的v1有156次数值查询、192次identity R mock、6次端点readout；v2新增24次数值查询、无新增R或readout。planagent此前2次v1边界探针和本次3次v2独立核查另列，不合并成执行者的调用量。

v2没有全量重跑v1的147项，只接受已审查的委托数学助手、修复差分及定向回归范围；不宣称任意schema/输入均已穷尽。原非平方范数包络分支没有被固定套件触发。此复核属于机械和条件数学检查，不代替真实数据上的独立科学复现。

AL18-N5-INVALID-FIRST与AL18-N2-EXPLICIT-K在v2范围关闭，v1历史缺陷永久保留。继续使用v1重放历史结果时仍应遵守其已记录限制。

下一包优先执行AL20紧凑强基线合同；并可独立推进AL24抽象匹配/凸目标软件参考。两者不读取真实数据，不运行模型/GPU，也不把已有数值检查称为新算法突破。
