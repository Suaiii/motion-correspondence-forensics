# AL87 planagent审查

冻结 v3 绑定 exact_bridge.py、reference.py、run_batch.py、config.json、input.json 与 AL79 reducer；v1/v2失败回执未被覆盖。v3证据哈希 `6074aac7d8d3e534a457921485c9c6744fb359e06ef30111a6121b094cd9cc23`。

推导仍使用 AL15 的四角色与 `d_a/d_b/K` 定义；精确身份只作用于跨调用输入 bytes，K 的浮点一致性单独使用预注册容差。有效输入的五项独立算术核对均通过，9类拒绝均通过，包含此前 AL86 指出的 one-ULP base-input 攻击。

替代解释：成功来自手工预计算的同一 trace 与既有 reducer，不能推出真实 probe、AE、来源或检测效能；v3仅修复软件身份合同。
