# AL55：OR1 唯一待证主张的最小新增信息计划

2026-09-19，planagent。AL55 仅为规划审查，不授权模型、媒体、服务器、GPU或真实标签实验。它把 AL54 保留的一个主张压缩成三层必要信息，不能将任一层“计划存在”写成证据已经获得。

## 算子层

必须先绑定两个真实 probe 的 wrapper/源码版本、权重字节摘要、posterior/dtype、cache reset、17 槽输入输出映射、共同支持/裁剪和端点误差；实际 project Python、磁盘、GPU 只读快照不能认证这些 wrapper 合同。缺任一身份或输出长度，候选停在 pending，不进入来源标签筛查。

## 信息层

同一合法输入和固定 probe 对同时保存 q 四项、稳定完整 r 十二项、普通单步/输入能量、分子/分母、固定评分、逐样本调用计数和误差/undecided 状态。q、h、eta、滤波、阈值和 probe bank 在 calibration 前固定，不能看 audit 标签选。强完整读出可直接计算 q 时，候选主张只剩有限读出归纳偏置；不能称新增可观察信息。

## 科学层

绑定真实祖先/来源/生成器/decoder/传播身份，预注册 fit/calibration/audit、五个实际随机源、strongest baseline 或完整固定基线集合、祖先簇重抽、invalid/coverage/fallback 和统计对象。主门槛维持 macro +0.03、配对95%下界>0、4/5种子为正，并通过静态、容量、增强、来源、采样、未见组件和代价对照。缺任何一项则 no-go/pending；不把开发池改成最终集。

这份计划不增加具体样本、不创建新模型、不购买资源、不改 AL05 fail；AL36 仍因研究 API 401 未派发。当前唯一合理动作是继续本地审查，直到外部研究执行状态发生真实变化或得到用户/导师科学决定。
