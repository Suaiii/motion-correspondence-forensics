# AL01 v2的限定审查与接续

2026-09-19，planagent。源提交5689d3e、81e7087、64325e8、9c594b5。
完整交接来自科研任务01a0b54a-3a46-72c0-9f2c-58bdb49c6424；包已完成，无遗留进程。

## 验收决定

AL01可按candidate_math_and_toy_falsification范围done/pass：接受明确的摘要碰撞、
二元及已知分组闭式替代证明、归一化的同目标数值恢复及浮点误差解释。
整体新颖性、真实检测增量和一般输入的优势仍inconclusive，不解锁真实实验。
这不等于通用软件输入合同全部通过；下述NumPy布尔类型问题保留为未关闭接口事项。

14项产物与9c594b5逐字节摘要一致；两次冻结均早于对应结果，且冻结提交中的文件匹配。
原参考和AL04摘要不变。合读初始failure_preserved与恢复pass_same_problem_recovery，
不改写初始三个Sinkhorn收敛失败，也不把更换数值求解器计为方法增益。

## 独立核验

从已保存Q、Gamma、对偶向量重算29份原始/对偶证书，未重跑LP。
对偶间隙最大1.25e-16，重算L1与报告目标最大差2.35e-11，均在声明精度内。
从原始特征自行重建12个正核，核对恢复Q的均匀质量和log缩放驻点关系；
质量误差最大4.38e-13。正核KL投影严格凸，满足约束的正缩放解支持同一目标的数值恢复，
不宣称在全部正核上该迭代都收敛。

三个摘要碰撞分别重新计算，其比较对象不同；未发现一个同时匹配完整简单统计向量的例子。
二元四面体下界与已知分组均匀提升上界相等的证明成立，这些例子不需要LP才能判定。
归一化改变Q，raw的非均匀列质量使其不属于该H-J接口，不能制造raw DJ对比。
真值1e-11对应浮点零的限制保留。

核验回执：[AL01_REVIEW_EVIDENCE_20260919.json](AL01_REVIEW_EVIDENCE_20260919.json)。

## 新发现的接口限制

针对源码审查后的两个实际探针，positive_temperature(np.bool_(True))以及
positive_temperature(np.array(True))均返回1.0；Python bool True则按合同拒绝。
原因是仅检查Python bool，没有覆盖NumPy布尔标量/零维数组。
编号AL01-NUMPY-BOOL。影响是输入类型合同不完整；本批固定float tau=.1结果不受影响。
不得称“所有布尔输入均已拒绝”。后续修复须新版本/新回执，不能改归档joint_v2或旧证据。
该小修补不阻断AL06的经典约束比较，也不能充当新科研成果。

## 接续

AL06检验未知分组的一般小型Q上，经典平衡二分证书的下界与遗漏；不再次跑本包的解析例。
planagent并行准备AL07：审查引入可见几何后，二次运动代价是否仍完全由两两边际决定，
以及非二次代价的可识别性所需信息。AL05在AL06后取舍，仍可明确拒绝所有当前候选。
