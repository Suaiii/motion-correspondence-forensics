# AL47：零假设探针边界设计完成（未执行）

2026-09-19，planagent。AL47 完成 `null_probe_envelope_design` 范围内的协议设计审查；没有运行模型、媒体、训练、服务器或 GPU，也没有选择 probe、h、eta、滤波器或标签。

设计按零对照、处理伪次序、读出盲方向、完整基线包含、同处理来源与跨组件迁移分别列条件；相同probe或已证明软P可交换时应当得到零，非零才提示实现或前提问题。通用非线性原R可交换不自动推出软P可交换，不能用它直接拒绝一个实现。每项均分开期望结果、可排除的解释和不能支持的主张。

草案要求在任何真实批次前固定 probe 身份、支持、dtype、缓存复位、分数方向、处理/来源双标签及 fallback，并禁止看到 audit 后扩充 probe bank。兼容性通过也不会放行真实机制：还需候选准入、预算/数据/祖先与时间证据、强基线、容量/静态/增强/来源/采样对照及五种子门槛。

AL47 仅提供最小可否证信息表，不能证明 OR1 的新颖性或性能。AL05、AL27、AL40 的失败/纠错记录保持；AL36 仍等待研究 API 401 鉴权恢复。AL48候选工作已被识别为重复AL16的评分误差传播，不继续增加同公式的pass清单。接续AL49核对非线性原R与软P交换性的区别，属于尚未覆盖的null前提问题。
