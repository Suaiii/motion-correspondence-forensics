# AL71：AIGVDBench公开 train/val 清单划分完整性审计

- 实际时间：2026-09-22T01:49:12+08:00
- 范围：完整读取公开 `train.jsonl` 与 `val.jsonl` 的元数据；没有读取 `test.jsonl`、视频、图片、权重或预测。
- 输入记录：`AL71_SPLIT_INTEGRITY_RECORD_20260922.json`。

## 结果

| 清单 | 字节 | 行数 | 有效 Video_id | 解析错误 | 缺失ID | 重复ID |
|---|---:|---:|---:|---:|---:|---:|
| train | 8,610,190 | 14,000 | 14,000 | 0 | 0 | 0 |
| val | 1,846,793 | 3,000 | 3,000 | 0 | 0 | 0 |

两个清单的精确 `Video_id` 交集为 **0**；train-only为14,000，val-only为3,000。两边每条记录都含 `Video_Prompts` 字段。响应和排序ID摘要哈希见JSON记录。

## 边界

这是发布文件的ID层划分完整性，通过项可以支持划分文件的可复算性。它没有检查test清单，也没有检查同一视觉祖先被切成不同ID、共享上游素材、标签来源、原生PTS/time_base、codec、许可或实际媒体是否可获得。前一轮AL70的20个validation ID与当前服务器12份开发manifest没有严格交集，因此公开数据不能被写成服务器已有开发池。

ID层无交集不能推出视觉祖先独立；它只解决文件标识重复这一层。正式数据gate保持 `not_evaluated`，机制实验与最终确认仍需AL68、来源/祖先合同和独立复核。AL36的OR1准入审查保持独立。

## 结论

AL71：**done / pass（train/val ID划分完整性范围）**。公开基准的划分文件本身可复算；数据权威性、标签有效性和机制可解释性尚未因此通过。
