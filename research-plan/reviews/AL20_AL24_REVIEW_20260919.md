# AL20/AL24：紧凑基线与有限训练子步骤审查

2026-09-19，planagent。审查bef69a4冻结及e5ae5e6完整交付；两个子任务分别按其范围通过，不释放创新、真实训练、数据或资源门槛。

## AL20：候选读出的精确包含关系成立

四调用已有量给出有限h恒等式K=d_a−d_b。每个共同线性滤波及共同支持上，候选能量可写成两个交叉增量能量减两倍内积。因此同分母的完整三元组能够精确重构q；只保留能量会遗漏对齐信息。12维读出基线z与原12维匹配描述b不同，不能混用或据新基线重选配对。

独立核对T^T T=6I及lambda/6对应惩罚相同子空间的关系。自由12参数目标包含4参数约束目标，但仅在同输入、配对、权重和度量下比较最优训练值；不能推断测试优劣，也不能把两者说成同容量。

核验7项交付摘要及冻结Git blob，从原AL15归档的三个固定trace重算精确关系。三例内积均0，第一和第三数值重复；这些限制已在交付中明确。一般内积项的结论来自代数证明，不能称非零内积已获模型实测支持。原q舍入差已保留，没有重跑core或选择新trace。

## AL24：小匹配和一维参考符合固定合同

核验11项交付摘要、源码及冻结输入。四图结果依次为：最大基数的交叉匹配/费用5/16、最小费用对角、两种零费用平局按ID选对角、空支持。两个非法输入正确拒绝，matcher不读取q。

读取归档解，以60位Decimal重新计算原数学目标、梯度和Hessian，和归档数值在1e-12内一致。M1首坐标约4.66512，M2约0.453026，符合事前解析范围；零差分点0与空表未定义分开。此诊断不是严格区间最优性证书，没有再次调用研究端优化器。

原包6次匹配、4次求解入口、100次值/梯度/Hessian求值、模型/probe均0。它只覆盖2×2图（硬限每侧4）和预定第一坐标子问题；不能称真实大规模匹配、完整四维拟合或检测器训练通过。

统一复核：[脚本](audit_al20_al24_20260919.py)、[回执](AL20_AL24_ARCHIVE_REVIEW_20260919.json)。这些是归档与数学检查，不是正式独立科学复现。

## 接续

增加AL25完成尚缺的固定四滤波张量读出及紧凑基线软件合同，所有输入仅为预定合成数组；明确相同支持和有限精度消去差异。AL26由planagent独立查核交叉重建/响应对齐近邻，以审查OR1剩余的归纳约束主张。两项互不等待，均不访问真实媒体/模型/GPU。

AL20使强基线更具体，但并未证明OR1泛化失败或成功。阶段突破仍未建立，AL05的历史fail不变。
