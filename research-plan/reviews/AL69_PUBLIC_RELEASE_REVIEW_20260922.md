# AL69：公开基准发布字段与标签语义核验

- 审查者：planagent；实际开始/完成：2026-09-22T01:40:03+08:00
- 输入记录：`research-plan/reviews/AL69_SOURCE_RECORD_20260922.json`，写入后 SHA256 `e4d1b7bf0c49e6c431dc20432ff33ab90b2942e47b21f603baecd286eb99c272`。
- 范围：GitHub 作者仓库与其链接的 Hugging Face 数据页的公开元数据；没有读取 test.jsonl，未下载视频、图片、权重或预测，没有 SSH/GPU/模型执行。

## 发布身份与划分

GitHub 仓库 `LongMa-2025/AIGVDBench` 的实际默认分支是 `main`。仓库 README 的固定响应为 5674 字节，SHA256 `cf5f379b56d490a1aad05f77a683846a70ac9ef3415d262a2939fd9dfa7a9962`。`data_splits/` 目录公开列出 `train.jsonl`、`val.jsonl` 和 `test.jsonl`。README声称：公开模型的2万条生成视频分成 train/validation/test，闭源模型只用于测试；仓库页面声称覆盖31个模型和40万级视频。这些是作者发布协议的可引用事实，不能替代样本级来源或时间验证。

GitHub 仓库API的 `license` 字段为 null。README感谢 OpenVid-HD 允许开放真实来源，但这段说明没有给出每个上游、版本、样本或再分发范围的机器可核对许可记录。Hugging Face 数据页可以访问，但本次只读取页面元数据，没有读取数据文件或测试内容。

## train/validation 清单的真实字段

按工作单上限读取：train最多8 MiB，val完整读取，test完全不读。

- `train.jsonl`声明大小为8,610,190字节，本次读取8 MiB前缀；前缀解析得到至少13,651行，出现1行解析失败，所以不能把13,651当作全文件行数。该前缀每条记录只有 `Video_id` 和 `Video_Prompts` 两个顶层字段。
- `val.jsonl`完整读取3,000行，0解析失败；每条记录同样只有 `Video_id` 和 `Video_Prompts`。
- 没有在这两份公开划分元数据中看到 `label`、`source`、`generator`、`ancestor_id`、`native_pts`、`time_base`、`codec`、`decoder`、`split`、`license` 或 `permission` 字段。文件名和prompt可以支持样本定位/内容条件描述，不能单独证明真实摄像来源、生成来源、祖先关系或时间协议。

## 按用途的裁决

| 用途 | 当前公开字段是否足够 | 可支持的结论 |
|---|---|---|
| 可比黑盒基准评测 | **部分足够** | 可以按作者划分和 `Video_id` 组织可比评测，但要披露公开元数据未提供的标签/来源/时间字段，并自行核查文件可获得性。 |
| OR1/时间机制归因 | **不足** | 不能仅凭 `Video_id`、prompt 和论文的模型表证明摄像祖先、解码器组件、原生时间或筛选选择机制。 |
| 最终确认集 | **不足** | 尚未证明祖先级不交集、许可、原生时间、标签凭据和未见来源闭包。 |

公开闭源模型评测不要求拿到闭源 checkpoint、VAE 或 seed；这些未知项必须在结论中保留。机制实验若声称 decoder/生成过程解释，则需要相应组件和处理链证据。公开发布的 train/val/test 文件证明划分文件存在，不能证明每条视频的祖先独立性；同一视频切段、共享素材或重复传播仍需独立审计。

## 对实验基底的更新判断

AL67的判断得到一个精确修正：当前数据基础已经从“只有论文和历史小清单”提升为“有公开的可引用划分和可定位的公开样本ID”。它仍然没有达到正式机制数据门。最小新增信息不需要全量下载：先在服务器上对选定 `Video_id` 的可获得文件做原生 PTS/time_base、文件哈希、codec/decoder和祖先/许可交叉核验；可比黑盒评测与机制归因的字段要求分别记录。AL68仍是现有开发视频的原生时间核验，AL36仍是OR1反方准入审查。

## 结论

- AL69：**done / pass（公开发布元数据审查范围）**。
- 公开划分可支持受限的黑盒基准复现规划；它不能单独释放 OR1 的机制实验或最终确认。
- 正式数据 gate 继续 `not_evaluated`。没有新增算法准入、效能数字或阶段突破结论。
