# AL57：作者实现合同审查

2026-09-19，planagent。该报告只核对了固定公开 commit 的三个代码/文本文件和一个训练脚本，共4份文件；未安装依赖、运行代码、下载权重/图片/数据或启动模型。它只判断源码合同是否闭合，不给出论文性能复现，也不放行OR1或真实批次。

## 固定来源

作者仓库为`HaoyueBaiZJU/genai-detect`，默认分支`main`在本轮固定到 commit `db5ceb1e66786039054f2bacb956611abf14b69d`，提交信息为`Update README.md`，提交日期2025-07-08。仓库README的引用给出CVPR 2025页码28821—28830；arXiv正文对应2505.01008v2。仓库大小元数据约20,983 KiB，未克隆仓库；本轮仅用GitHub API树和4个raw文件。

来源记录：`AL57_SOURCE_RECORD_20260919.json`。记录包含API/raw URL、UTC取得时间、字节数、SHA256及Git blob SHA1；四个文件总计约16.7 KiB。临时源码缓存已删除，未把源码副本放进仓库。

## 已闭合的调用链事实

`eval_fake_vs_real.py`行23—88定义每图评估。输入被RGB转换并resize到512×512（42），`mask_image`参数可传入但主流程行188—213显式传`None`，所以该入口的默认调用没有实际遮挡。每个输入循环`num_gen`次，默认3（29、247），每次设置`torch.manual_seed(seeds+i)`（51—53）并调用`StableDiffusionInpaintPipeline`（53—59）；默认推理步数50、guidance scale7（31—32、253—254）。三次输出先stack后平均（65—66），输入也只是同一图复制3份再平均（46—48）。

评分使用`mean_gt_sample`和`mean_outputs`（71—81）。代码以`pil_to_tensor(...).float()`保留0—255量级（44、62），但PSNR对象声明`data_range=1.0`（75），MS-SSIM声明`data_range=2.0`（69）；因此实际数值范围与论文附录B所写8-bit/MAX叙述并未在此入口闭合。L1和MSE（代码变量l2）是同一平均输出的逐像素损失（80—81）。这足以说明“先对3次输出平均再算分数”，不能推断作者其他脚本或论文图表全部如此。

主函数加载基础`botp/stable-diffusion-v1-5-inpainting`（177—180），命令行声明LoRA路径和scale（237、250），但在该commit的这一入口没有调用`patch_pipe`或`load/tune`来加载LoRA；仅仅导入了相应符号（14）。因此代码文本没有闭合论文所述“对齐替代模型已用于检测”的执行链。训练脚本另行存在，但不能用入口中的未调用参数替代加载事实。

`training_scripts/inpainting_lora.sh`行1—40明确是一个训练轨道：SD inpainting模型、`./data/ldm100_0.5k`实例目录、`train_inpainting`、512分辨率、batch1、累计步2、UNet/text/TI学习率、LoRA rank8、最大TI与tuning步数各3000、CUDA设备`cuda:1`。它支持“需要目标生成样本/训练配置”的权限判断，但不能证明脚本曾经成功运行或论文使用的精确数据、步数和墙钟成本。README行31—49要求另外下载真实/伪造/mask数据，行59—67给出50步、guidance7的评估入口；本轮未下载。

`utils/metric_utils.py`行100—124把传入的正类分数放在labels前段，直接用sklearn计算AUROC/AUPR，并以`recall_level_default=0.95`计算FPR。`eval_fake_vs_real.py`行217—227调用`get_measures(fake_psnr, real_psnr)`，随后把`real_psnr`标0、`fake_psnr`标1计算mAP；这确认了代码入口的分数方向。代码中没有把K作为命令行参数，K在这里对应`num_gen`默认3，且先平均输出后评分。该入口的主流程mask为None，所以它无法作为论文masked恢复路径的完整实现证书。

## 与论文和OR1的边界

论文§3.1—3.2描述遮挡条件恢复、K次随机恢复以及目标API样本上的LoRA对齐。固定代码只闭合了“重复生成—均值—分数”的一条评估链；它显示了主入口的默认mask缺失和LoRA加载缺口，同时保留训练脚本确实存在的目标样本/训练轨道。README、脚本和入口之间不能无条件合并为一条已运行的论文管线。

这项先例的实际主动算子仍是条件扩散/inpainting恢复，调用成本包含每次50步扩散推理及可能的训练；OR1是两个固定完整重建器的软步交换、共享前级后的4次E+D调用。两者没有相同调用单位，也没有同一标签、时间支持或未见decoder合同。该先例因此扩大了“恢复检测”近邻风险，但没有证明OR1软交换q已被同一方法覆盖，亦没有为OR1增加任何新信息或性能证据。

PSNR/MSE的排序问题只能得到有限诊断：固定入口先平均3个输出，再对平均张量评分；输入量级、PSNR data_range和mask实际传递存在代码与文档的合同缺口。没有执行，不能把缺口升级为作者结果无效，也不能据此选择有利分数。后续若需比较，必须分别固定遮挡支持、随机种子、聚合顺序、MAX/归一化、目标样本权限和推理步数，并把未加载LoRA的入口与对齐版本分表。

## 结论与下一步

AL57的源码合同审查为**有限通过、科学比较仍未验收**：四份固定文本足以确认恢复调用、默认3次平均、入口mask为None、训练脚本的目标样本轨道和若干参数；论文版本映射、真正masked评估、LoRA加载、完整成本和作者结果复现仍未闭合。AL56的文献取舍不变：恢复检测已有直接先例，OR1依旧只保留一个待证归纳偏置假设，正式创新和真实实验均未放行。

保持AL36为独立反方准入审查，等待研究端已记录的401鉴权状态发生真实变化；不重发、不换身份。若后续还需一个最小独立问题，应针对本次发现的**入口mask为None与论文masked轨道不一致**，在权限和版本冻结后由不同审查者复核，不再重复一般最小信息计划。
