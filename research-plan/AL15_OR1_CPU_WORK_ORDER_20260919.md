# AL15：OR1固定CPU核心与调用合同

仅在主DAG本包已派发后执行。researchagent使用当前独立工作树。
依据AL12_REVIEW_20260919.md，仅放行软件/代数与mock合同，不放行新颖性、真实机制或模型。

冻结源：a4cfdeb的AL12_method_design.md及AL12_cpu_preregistration.json；
后者SHA256为91e856957b34beb14c3f098ce7f078594cb0529268367ec120f3eb1da2f78292。
原JSON保持draft_not_authorized历史字节；本工作单和新运行记录表达当前授权，不能追改旧文件。

实现P_j^h(u)=(1-h)u+hR_j(u)与两条交换顺序，保存4次总调用的DAG、全部端点、K及标量/范数控制。
只运行草案8族及其固定2/3→4/8维嵌入/坐标置换；h=1/4,1/8,1/16全部报告，主h=1/8。
维数<=8，单数组<=1MiB，NumPy/stdlib、2线程；无Torch、图像/视频滤波实验、媒体、模型、GPU、服务器或拟合。
向量范数的sum/mean及eta缩放必须明示，不能在嵌入维数改变时悄悄改变等价预期。

必须检查：

- 线性精确关系、常量平移、可交换零响应、同单步标量误差例、交换反号与能量不变。
- 非线性余项按已给解析式和界逐值核验；不能仅看拟合斜率或图形走势。
- 非交换但被普通误差解释、相同输出经验分布不可分的失败保留，不训练toy真假分类器。
- 每路径a/b各一次，总计4次调用；同h/支持/条件，probe不读取生产decoder身份、来源标签或真实latent。
- 输出槽位/声明时钟映射、有限值、确定性/缓存策略的mock准入；不把mock通过当真实wrapper已验收。
- 每个R调用使用声明一致的独立初始缓存，不能让另一路遗留状态混入。若做额外随机性/精度预检，单列诊断调用，不能暗中加调用仍声称总量4。
- C/R/R2与G/G2来源标签分别保持，处理属性另记；共同最终传播在base arms之后，soft中间无有损写盘。

先保存实现、配置、全部实际输入与预期、外部依赖摘要，独立提交冻结后运行一次。
失败保留，修复另立版本/回执；不在这些例子上优化参数，不增随机搜索，不报AUROC。
真实四滤波读出、训练配对logistic及视频接口仅注明pending，不需要为了本包补实现。

产物：research-runtime/algorithm_candidates/or1_cpu下的独立实现；
research-runs/algorithm_search_20260918/AL15_report.md、AL15_evidence.json、AL15_handoff.md与清单。
回报边界、所有失败、实际资源及下一步；主DAG/旧证据只读，提交独立分支后由planagent审查整合。
