# AL23：OR1匹配、凸读出与种子解释的数学合同

planagent独立审查；不执行AL20交叉响应任务。输入AL12固定训练定义、AL21评价合同；不等待AL18实现或真实资源。

目的：完成算法训练阶段尚未实现部分的可复核合同，避免后续匹配/优化选择改变研究问题。仅数学与固定小表，无真实特征、分类器拟合或toy检测指标。

限定问题：

1. 在已冻结配对集上，推导带1e-3平方正则的四权重pairwise logistic目标的梯度/Hessian、严格凸性和零差分输入下的行为。区分损失的普通已知性质与新算法贡献。
2. 展示最大基数且最小费用匹配存在平局时，两个合法配对可能产生不同排序目标；固定四个抽象非负q标量：正组[3,1]、负组[2,0]，所有b相同且非零。比较配对差分[1,1]与[3,-1]，仅推导并在固定w=1检查梯度方向，不拟合参数或赋予真实probe可实现性。要求记录最终配对ID与权重，而非只记录匹配输入。
3. 核查固定probe、确定性匹配和严格凸读出下五种子结果的含义。相同确定性解复制五次不能当独立稳健性证据；不自行降低阶段门槛，先列正式验证中需声明的随机环节和比较范围。

产物reviews/AL23_MATCHING_OBJECTIVE_REVIEW_20260919.md。验收仅 `matching_and_optimization_contract`。不修改已冻结方法/正则/匹配规则，不训练、服务器、媒体、权重或GPU；不宣称获得检测增益。
