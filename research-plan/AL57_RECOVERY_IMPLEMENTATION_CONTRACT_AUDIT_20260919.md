# AL57：恢复检测原作者实现的评分与成本合同

planagent，只读审查。输入是AL56已定位论文arXiv2505.01008v2及其第1页所链接的作者仓库`https://github.com/HaoyueBaiZJU/genai-detect`，AL12、AL35和AL54的同权限比较条件。独立于尚未派发的AL36，不需要科研端启动或真实资源。

科学问题：论文PSNR/L2消融的排序差异，能否由实际采样、支持、数值范围和聚合合同解释？原生目标对齐检测的可用代码是否足以确定其目标样本权限及完整调用成本？结果直接决定它是否能成为可复核的原生/匹配比较臂，而非制造新算法主张。

边界：只读公开Git元数据和目录清单，首先固定commit SHA，随后至多读取4份该commit下的README/检测入口/评分/配置文本，总量不超过512KiB。元数据和文件各记录真实URL、时间、字节数与SHA256；文件不足或代码缺失如实停止。不得克隆大仓库、递归取全文、下载权重/图片/数据、安装依赖、执行作者代码、生成媒体、接触服务器或GPU。遇实际登录/权限限制不绕过。

需要核对：K与随机源，补全内部去噪步骤和可复用前级，mask评分支持，固定MAX/归一化/裁剪，PSNR与MSE的分数方向，先平均还是先变换，N与LoRA训练是否有可追溯配置，目标样本接触和严格未见目标的差别。只由已读调用链计数；未知值保持unknown，注释和README声称不能替代可达代码。

输出`reviews/AL57_RECOVERY_IMPLEMENTATION_CONTRACT_REVIEW_20260919.md`及紧凑源码记录。明确哪些论文配置能从该commit映射、哪些不能；不要假定当前默认分支就是论文当时版本。若同一单次MSE/PSNR却评分相反，指出具体行与条件；若存在K次非线性聚合，则说明不同统计对象，不径直判bug。不改作者代码或OR1冻结实现，不汇报未经执行的指标。

失败条件：仓库不可用、4份文件无法闭合调用链、论文版本映射缺失或关键参数不可证时，交缺口并保持对应比较臂未验收；有限文件未发现问题不构成完整复现通过。准入结果仅限源码合同，不释放模型、真实数据、预算或正式创新门槛。交接后保留AL36及至少一项由本次实际发现产生的独立后续问题；避免继续重复一般协议模板。
