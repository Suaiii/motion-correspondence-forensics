# AL21：数值拒绝与匹配支持对评价结论的影响

待执行planagent，独立于AL18/AL20。输入AL12匹配/coverage定义、AL13来源协议及AL16的未认证状态；本任务不修改研究端实现。

要检验的风险：数值/支持拒绝改变被评分人群，即使两类保留比例相同，也可能抬高条件AUROC。原宏增益门槛不能只在各自最有利的接受集上比较。

限定工作：给一个事先固定的小型分数/接受掩码反例，完整列所有正负对而不拟合模型；区分接受集条件指标、联合覆盖、统一输入集合及预定fallback后的完整系统指标。说明真实样本FPR的条件/总体分母差异，零假设和祖先重采样单位，以及同一对样本和共享真实源的要求。审查训练caliper截断与推断拒绝是两种不同选择过程，不把共同支持当可部署全部分布。

输出reviews/AL21_COVERAGE_EVALUATION_REVIEW_20260919.md及必要精确表。准入仅 `coverage_evaluation_contract`。不得调整真实阈值、重开最终集、读取真实媒体或训练；不需要等待AL18结果，也不替代实际来源覆盖和增益验证。主DAG标明开始后再执行。
