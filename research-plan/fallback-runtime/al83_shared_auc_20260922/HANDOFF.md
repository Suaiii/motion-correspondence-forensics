# AL83 handoff — full contract failed, raw runs preserved

AL83 attempted a new shared-negative/ancestor-weighted paired macro software contract. The current v5 evidence has `software_pass=false` with four failed checks: invalid sentinel/reference equivalence, expected tag-a difference, and ancestry-weight effect. Earlier v1–v4 failed during execution (key mismatch, absent score block, JSON serialization); their run directories/freezes remain as failure history.

The implemented API uses sample-keyed weights and has not completed the promised ancestor-keyed public interface, strict record validation, exact two-U numerator/denominator records, complete reject suite, full fixed fixture configuration, or mean_delta output. The sorted path still has per-positive scans and is not the promised O(n log n) algorithm. Do not use it as the evaluator for real research.

AL83 should remain execution=done, gate_result=fail. Raw v5 numbers are diagnostic only and do not establish a valid point-estimator contract, CI, bootstrap or efficacy. AL81 remains fail; AL82 remains blocked. No media/model/network/server/GPU was used. AL84 remains a separate prepared CPU identity-bridge task and is not blocked by this failure.
