# AL83 protocol

This is a standard-library point-estimator contract. All generator groups compare against one shared negative pool; ancestor weights are shared across method/tag/generator and same-ancestor cross-class pairs retain the product weight. Missing support returns null; no group is dropped. The Fraction oracle is independent from the sorted implementation. There are no confidence intervals, bootstrap, real scores, media, models, server or GPU calls.
