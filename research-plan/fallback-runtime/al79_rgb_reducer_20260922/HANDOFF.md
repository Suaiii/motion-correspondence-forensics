# AL79交接：逐样本真实时间分块归约

planagent独立fallback实现；冻结提交6847224。AL78永久保留done/fail，原文件8项受保护摘要前后相同。科研线程没有被重发，也没有创建软件agent。

## 执行与证据

- 实际运行：2026-09-22T07:19:35.105+08:00 至 2026-09-22T07:19:35.515+08:00；wall 0.410668s，processCPU 0.390625s。
- 一次冻结正式批次；90项实际比较/拒绝检查，0失败，0异常；未删除或覆盖run_v1。
- 先静态语法审阅修正索引/数组生成，再冻结源码；没有冻结前数值预跑。
- Python 3.12.14, NumPy 2.3.5；四项原生线程环境上限2，没有BLAS线性代数或Python子线程。
- 有效输入最大单数组626688字节；RSS未测。拒绝超限用3个标量支持的broadcast逻辑视图，不分配大数组。
- 命令：`python -X utf8 research-plan/fallback-runtime/al79_rgb_reducer_20260922/run_batch.py run`。
- 46次reducer调用（含20个拒绝病例），3次独立Fraction参考，对C1的两个样本和C2/C6逐滤波核对；没有网络/服务器/媒体/模型/GPU/优化器。

## 验证结果

- 结果按B逐样本输出q4/r12，换B顺序和单样本独立调用一致，不再输出跨B聚合的分类输入。
- 块内真实切片。17帧例子过滤5/7/5帧三个块，整段/分块/两布局数值一致。空间valid内区与AL25相同；S沿用AL32的Hd_a+Hd_b。
- Fraction直接核对元素数、五个能量和、分母、q及r，全部误差满足原atol=1e-12,rtol=1e-11；全批数值检查最大绝对误差3.5527136788005009e-15。
- q与r_K逐项float.hex相同。近抵消identity q=1.1102230246246014e-16，未裁为零。全零场仅保留eta分母；相反d场的rS/rJ严格零，独立参考保留负AB交叉项。
- 不等长块反例：平均块比值0.36950323070071145，正确全样本值0.025603511338697846，差0.34389971936201358。该例直接拒绝平均块q的替代实现。
- 20种非法输入各核对预期异常类型/错误码；包括缺失字段、无效块、错布局、NaN/Inf、超限与中间溢出。

## 边界与后续

仅软件接口通过。Fraction是同执行者的独立算法实现，不能当作另一研究者的科学复核。CPU合成场没有来源标签，不产生AUROC或模型效果。真实AE的输出/缓存/颜色/精度合同仍未验证；分块只作用于已得到的场，不授权切分真实AE的时间上下文。
AL80准备只读检查现有服务器的AE配置/权重文件身份；AL68媒体时间核验与AL36反方准入仍独立保留，provider故障不重试。真正模型/效能批次另立准入。

## 文件摘要

```json
{
  "reducer.py": "f15575026b8de1723aa0122569fde70d1b9596c0e92aab8508862f838a8fd679",
  "reference.py": "dbc61b1f851950e4506121d3d1683949761be67f4afd5ef0e8f792956e8fc36f",
  "run_batch.py": "d5f6ff486ac5b3d0dba071d591e9671afb8e7d0314a596d5cb2527cebe2aae41",
  "config.json": "139c8bcfbd4a5113a539354a1e164abbd4615a4f7953bb4e57e2ef34da9cf620",
  "freeze.json": "7fdf8de963ca28c3f20e3891011e005d899fdca2baa57500a87d76000f7aba43",
  "run_v1/started.json": "36f7bb9b23cfd581ab43e460aedca081b9e741747c5b85ce7f840053e6c7374e",
  "run_v1/evidence.json": "ef2cc7ab7c9c886a0bb0350278ab76ec7a5a3d5ba0d61d1b5d63c646fcf13961"
}
```
