# AL43：OR1 能量读出对局部偏移的敏感性条件

负责人 planagent，独立于 AL36/AL42。输入 AL12 的 H-OR 局部偏移假设、AL20/AL29 完整基线和 AL16 的数值误差边界。仅符号数学/条件审查，不用已暴露数据或重跑原合成场。

待核查假设：即使局部 J_K delta 不为0，实际四维能量比 q 是否就有一阶信号？固定 H、共同支持，N(u)=mean||H K(u)||²，D(u)=mean||H v_a||²+mean||H v_b||²+eta。假定局部 K/v 可微且 D>0；先声明这些条件不能自动套到缓存、裁剪或非光滑实际AE。

推导任意方向 delta 的 Dq，分开分子交互变化与分母变化；尤其检查 K(u)=0 时，一阶项是否必为0。若 K 可作二阶展开、D(u)>0，求小偏移 u+epsilon delta 的首个非零阶及正负方向的对称性；不使用 epsilon sweep 或人工标签性能。

验收要求：明确 J_K delta!=0 并非 J_q delta!=0 的充分条件；若成立则削弱 AL12 中将场敏感性直接转成读出敏感性的论证，不能直接宣告真实 OR1被否证。若有非零K的正交盲方向，给符号条件，不事后搜索有利例子。保留 q 被稳定完整读出包含、真实误差 unknown 与科学准入未过。

交付 AL43_OR1_ENERGY_SENSITIVITY_REVIEW_20260919.md，包含推导、需要核实的假设、相同输入基线及下一最小可检验信息；不创建新候选或主张经典导数公式原创。无源码部署、模型/权重/媒体、训练、服务器/GPU，预算门槛不变。
