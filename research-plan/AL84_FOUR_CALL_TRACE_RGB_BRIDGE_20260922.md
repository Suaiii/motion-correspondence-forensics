# AL84：四调用输出到RGB归约的身份桥接（待本地接续）

planagent在独立fallback-runtime实施预批准CPU接口工作，依赖AL15/AL79的算子与归约合同，独立于AL83统计原语。先读取AL12、AL15/18、AL79冻结产物与AL80限定资产观察；不复跑原数值toy或假装已有真实AE。

具体问题：AL79输入的是已算好的d_a/d_b/v_a/v_b，尚无从四调用记录到这些场的可核对输入身份。实现仅接收外部提供的浮点RGB输入与四份输出的纯函数，绑定sample_id、probe_a/probe_b ID、h、角色和张量哈希；拒绝错sample、错probe/阶段、张量布局/shape/时槽不符、重复/缺少记录。不加载或调用任何重建器。

按AL12路径次序明确两次第一步与两次外层调用的输入张量哈希，计算软混合端点与K；d_a/d_b的定义须先从AL15已存合同读取，不用想当然的端点/h²替代已有定义。输出AL79需要的RGB场并字面共享q/r输入。使用至多两个固定小型RGB解析输入、四个显式已给定映射结果，核对直接路径与稳定字段关系，另行冻结身份破坏反例。禁止真实权重、媒体、网络、SSH/GPU、分类拟合或API访问。

交付源码/配置/输入hash、单次批次证据与handoff。数学假设、有限精度误差与输入角色分别报告；接口通过不认证真实probe，也不释放AL36、数据或模型门槛。开始前补足精确字段映射与预期，避免重复AL79归约测试；出现已有产物完全覆盖时去重结项而不加新pass。
