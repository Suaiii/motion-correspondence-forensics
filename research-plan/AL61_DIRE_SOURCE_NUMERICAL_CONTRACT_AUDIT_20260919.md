# AL61：DIRE源码的调用路径、clipping与残差表示

planagent，独立于AL36。AL60已确认DIRE2303.09295v1的公共ADM/DDIM反演—重建—绝对残差—分类器方法。正文的步数单位和有限步误差解释尚不能绑定具体实现；本包仅补这一个实际合同。

输入为AL60报告/来源记录、AL12/AL35同调用与来源对照。论文第1页链接作者仓库`https://github.com/ZhendongWang6/DIRE`。首先读取公开Git元数据/目录清单固定commit；随后至多4份纯文本，合计<=512KiB，优先README、DIRE计算入口及实际被调用的DDIM核心/残差预处理。不能以当前默认commit自动等同论文版本，记录映射证据或unknown。

核对反演和重建是否各自循环S次、scheduler时间重映射、clip_denoised或其他clamp，去噪器调用单位、残差是否绝对值、float到文件的量化/缩放、分类器读取前后顺序。只对4份已读文件闭合路径作判断；依赖未读写unknown，不能据参数名断定行为。论文生成数据50步与检测20步分列；计划调用数和实际运行成本分列。

只读公开文本，不克隆大仓库、不取权重/残差图/数据、不安装依赖、不执行作者代码或自建toy、不改OR1代码，不访问服务器/GPU。哈希计算与文本解析仅作来源校验，不能作为科学实验。真实权限阻断不换身份或绕过。

交付`reviews/AL61_DIRE_SOURCE_NUMERICAL_CONTRACT_REVIEW_20260919.md`与`AL61_SOURCE_RECORD_20260919.json`，包含commit/URL/时间/字节/SHA256/Git blob、实际源码行、路径未闭合处及对强比较臂的影响。4份不足则交部分审查，不无限追加文件；源码存在/语法正确不代表复现成功。若明确路径不符，相关比较臂合同fail，同时保留论文先例，不能据此推定OR1优势。

不提出未经实测的模型成本，不判断源类效果；若算法/数据/预算未通过，模型运行仍不允许。交接保留AL36及一项由新发现产生的独立工作，避免用相同推导或模板填队列。
