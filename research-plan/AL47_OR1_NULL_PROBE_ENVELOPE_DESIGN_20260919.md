# AL47：零假设探针与实际读出功效的最小区分设计

planagent，独立于AL36的准入评审和AL45的外部来源核验。输入AL12、AL19、AL35、AL43；只做方案与可否证条件设计，不执行模型/媒体/训练/服务器。

问题：OR1测试时probe由研究者选择。单凭探针非交换或理论上有delta方向无法说明来源信息；需要在看真实audit前给出不依赖来源标签的有效/无效probe边界。为候选银行准入设计一个有限、可拒绝的条件表，而不是调probe直到高分。

至少区分：两probe相同/可交换导致K恒零；夹断与缓存造成伪次序；K非零但实际q/固定头盲方向；q被完整强基线包含；以及真实R/G同处理链、跨来源/未见decoder的数据条件。每个条目写输入权限、必须测量的量、可排除的替代解释、还不能支持的主张和失败后下一步。

使用已有解析负例和符号结论做设计输入，不重新执行或扩大旧场矩阵，不挑新人工标签、不选择最优h/eta/核。最多提出一个未来有意义的prelabel兼容性检查包，并给停机/信息不足条件；它必须与有标签机制批次分开。若仅有冗余的检查项，明确删除，不把增加报告篇数作为进展。

输出 AL47_OR1_NULL_PROBE_ENVELOPE_DESIGN_20260919.md（reviews目录）与待绑定真实字段，不声称probe已选中或条件表已执行。当前资源/数据/创新依赖未满足，依旧不授权模型下载、真实采集或任何GPU批次。
