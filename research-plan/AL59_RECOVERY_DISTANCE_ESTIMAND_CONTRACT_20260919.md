# AL59：恢复距离差的可比估计量与无效规则合同

> 状态：本轮未登记的提案已撤回，未派发、未执行，不能用作当前工作单。AL58最终审查发现其与AL13/35/37规划重复；匹配恢复方差后信号消失不自动否定来源信息。保留此草稿仅为解释放弃的提案。下一实际登记任务是AL60 DIRE正文审查。

planagent，承接AL58的方差分解和有限支持反例，独立于阻塞的AL36。该包只写一个可执行的审查合同，不运行作者代码、模型、媒体、服务器、GPU或真实视频。

## 科学问题

在固定`X`、mask、目标decoder/恢复器、输出支持和随机源后，如何分别估计逐次误差`A`、均值图像误差`B`、PSNR及恢复离散项，使来源差异不被恢复方差、处理链、目标接触或评分方向解释？若同一恢复法下的来源间隔不能在这些条件下定义，恢复检测臂应保持no-go。

## 输入与边界

只使用AL56/AL57/AL58已写的对象定义、来源权限和失败条件。固定来源/祖先/decoder、相同mask与像素支持、同一`K`、随机种子合同、输出裁剪/量化和PSNR `MAX`。把每个样本的`A`、`B`、`A−B`、PSNR、生成调用数、训练/目标API接触和有效覆盖率分列；不以PSNR替代MSE，不把`A−B`当来源特征。

## 工作与可否证条件

1. 写出按样本、按来源、按decoder/处理层和按祖先配对的estimand；明确先算样本分数再聚合与先平均恢复再评分的区别。
2. 设计一个不依赖模型运行的无效规则：若来源差异在匹配`A−B`或恢复协方差后消失、方向随随机源/聚合顺序改变、或目标接触权限不一致，则比较臂不具备可解释的来源增量。
3. 区分可报告的统计协议和尚未满足的数据门槛；不能把预注册合同写成效果证据。

失败条件：该合同仅重述AL58恒等式、需要未知真实样本身份才能定义、或引入不同类别的mask/恢复器来制造间隔；此时交去重记录并关闭，不另设实验。

## 交付

`research-plan/reviews/AL59_RECOVERY_DISTANCE_ESTIMAND_REVIEW_20260919.md`与`AL59_MANUAL_REVIEW_RECORD_20260919.json`。交付应包含输入哈希、版本化定义、缺失字段及一个明确的go/no-go使用边界。该合同不放行OR1、恢复检测先例或任何GPU/真实批次。
