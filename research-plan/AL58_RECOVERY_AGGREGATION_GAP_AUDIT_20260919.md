# AL58：随机恢复聚合的方差项与来源间隔

planagent，独立于AL36。输入仅为AL56论文所读§3.1/附录A.2及AL57固定源码的平均图像后评分。AL57的比较臂合同fail保留；这里分析已观察到的两个数学对象，不依赖其源码通过，不修作者程序。

可否证问题：K次恢复的平均平方距离与平均恢复图像的平方距离能否互换，且“恢复器更适合自身生成分布”能否保证生成样本得分更好？该问题直接影响恢复强基线和待证算法机制，不能仅用恢复次数或PSNR改名解决。

边界与固定对象：同一个mask/固定共同支持、维数d，参考y及恢复Y1…YK；K>=2。区分给定y,X的有限样本恒等式和条件独立同分布、有限二阶矩下的期望。固定比较A=mean_k||y-Yk||²/d、B=||y-mean_kYk||²/d；不读真实图像，不运行随机模拟或作者代码，不新增第三种候选头。PSNR与负MSE固定MAX的排名等价已由AL56/57提出，本包不把重述该等价当新结果。

交付：手工推导A-B与恢复离散程度的恒等关系；条件期望中bias与方差权重；若共享随机源/非独立则明确哪些式子不适用。构造至多一个有限支持、固定同一已知区域X的源分布反例，检验生成过程与恢复器同分布是否足够推出正向距离间隔；对象必须明示，不能借源类不公平的不同mask或不同恢复器。若不能反例则只记录条件式，不编造失败或成功。

输出`reviews/AL58_RECOVERY_AGGREGATION_GAP_REVIEW_20260919.md`与输入哈希/人工推导记录。经典方差分解不称新算法，源分布toy不称真实检测AUROC，也不外推至OR1确定性4调用。明确条件均值相同、方差/多模态或补全方差可解释的范围，给最多一项能改变现有机制判断的下一任务；保留AL36独立复核。

执行范围为数学/文献审查，不采用执行结果布尔表充当证明，不运行模型、媒体、服务器、GPU或训练，不解除任何真实与创新门槛。失败条件是只能重复已写结论、无法给出明确对象、或需未知真实条件才判定；如重复则去重结项，不计新pass。
