# 科研专项执行窗口交接（2026-09-20）

授权：用户多次明确要求新科研冲击窗口，并说明现有服务器仍开机。Plan9.16继续维护主计划、审查与DAG，新任务负责科研代码/批次。过去创建收据只有clientID不等于任务已运行；旧会话错误、归档和当前接管状态须分别记录。

## 首包

dispatch_id：cc-research-window-al36-al65-20260920；task_ids：AL36、AL65。两项依赖独立，可依次完成；AL36的no-go不阻塞AL65。

- AL36：沿用原工作单，加入AL54最终矩阵、AL56/57/60/62重建先例、AL63/64数据筛选证据。进行不同执行者的算法反方审查，不重跑旧原型。不给自己执行的AL65盖独立科学复核章。
- AL65：按AL65_SELECTION_MECHANISM_CPU_20260920.md冻结五个抽象概率表情形，交付标准库Fraction的可执行CPU模块、逐格证据、原失败及实际源码/配置哈希。不能改成文献/任务清单交付。

先读取主工作区 E:/aNB/TECH/脉冲神经网络 的AGENTS.md、agents/researchagent-next/agent.md、WORK_PLAN、CONTINUOUS_RESEARCH、project.json和本交接。工作树计划副本可能滞后；本交接取代角色文件中旧AL04首包指令。若在主DAG仍看到旧线程ID，报告新任务ID/worktree供planagent绑定，按照本用户授权包进行，不能自行覆盖主DAG。

## 写入和资源

仅在自己的独立Git工作树写科研源码/报告；分支使用codex/前缀。AL65输出research-runtime/algorithm_candidates/selection_controls与research-runs/algorithm_search_20260920，AL36按原输出路径。不得向主master直接推送、覆盖现有文件或修改证据；完成提供提交与文件清单给Plan9.16整合。

AL65允许小型CPU代码，不允许模型/真实媒体/权重/数据manifest/最终集或训练；费用、来源与真实模型准入仍分别待核实。00:45左右SSH只读观测：13340在线，RTX4090显存/利用率为0，无GPU计算进程，12核/90GiB，数据盘空余14318481408字节。该观测已经保存，不重复SSH以制造进度，不为利用率启动未批准训练。实际报价/历史用量未知，不写零，不再重复索要已有预算问题。服务器继续开机是用户提供的状态，不替代一次具体付费实验授权。

## 必须保留

AL05/H-J/H-T创新未准入；C与旧100条暴露数据冻结；OR1没有真实跨来源效果，q被同四调用稳定r包含；AL27/40/49/50/57/58失败保留。软件/经典反例不算CCF-A突破。服务器或任务API故障只阻塞有关支路，CPU与独立算法问题可以继续。

科研旧会话01a0b54a末次尝试返回function_call_output requires call_id，无产物；不同的已归档新任务01a0ba4b曾报默认gpt-5.4不支持。不得混称同一个错误、重复发送或擅自改账户/组织/模型。使用本次创建的默认模型；若仍启动失败，交精确错误且不无限重试。当前任务之外不自动创建更多任务。

## 完整交接

首个回复确认实际task/worktree/首包及输入范围，随后直接执行。包完整时给Plan9.16一次实质交接，附源码/配置/数据（仅抽象概率表）哈希、命令与时间、未改门槛、失败说明、可复算产物、两项独立后续建议。必要的后续研究由主DAG安排，不能让旧AL63/64再次成为新窗口任务。
