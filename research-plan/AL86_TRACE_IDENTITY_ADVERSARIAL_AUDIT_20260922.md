# AL86：四调用记录身份的定点反方审查

planagent 审查已有 AL84 原始字节，不修改或重新运行其批次。限定一个新增攻击：修改 Rb_x 的输入为与 Ra_x 输入相差一个 float64 ULP 的值，同时更新该记录自己的 SHA256。人工沿 bridge.py 的控制流检查是否拒绝；区分输入身份的精确字节绑定和数值结果容差。核对 reference.py 是否构成不同算法参考，冻结时间/运行次数/预期是否有实际回执；不把源码静态检查等同实际模型验证。

输入为 AL84 工作单、source/config/input/freezes/evidence/handoff 和 AL20/AL25 的字段定义。交付带真实文件哈希的审查，清楚列出可保留的观察和需撤回的验收；不制造运行统计或假装独立于主实现者。没有新媒体、模型、网络、服务器、GPU或数值批次。

若确认身份漏洞，后续仅准备独立的精确身份检查修复包，冻结攻击与正确输入后再运行；原 AL84 fail 不回改。该审查与 AL85 点估计修复无成功依赖。
