# AL16：OR1的有限精度与无信息域合同

planagent独立数学审查，与AL15实现并行，不修改其代码/输入。
仅采用已交付OR1公式，不运行实际AE或拟合检测器。

问题：K=(u_ab-u_ba)/h²和能量比q_l的数值误差，何时足以淹没所声称信号？

1. 给出已知端点绝对误差时K误差的上界，区分端点级误差与单次R_j调用级误差。
   若用R_j的局部Lipschitz常数传播probe调用误差，明确常数未知时不能给真实保证。
2. 对线性滤波后的平方能量及分母d=e_al+e_bl+eta推导比值误差界，写明d>误差界等必要条件。
   分母加小eta不等于噪声安全；无信息/数值不稳定必须显式拒绝或覆盖报告，不能低分判真。
3. 给出主h=1/8与预定另外两步长的放大因子，并区分精度检查与后验选最有利h。
   确定性重复输出相同只能排除某些随机性，不能证明舍入准确；真实低精度需独立高精度/误差估计。
4. 说明怎样将这些界变成未来profile的字段和拒绝条件。未知误差/Lipschitz常数保持unknown，
   不用本机float64的机械检查证明真实GPU半精度安全。

产物research-plan/reviews/AL16_OR1_NUMERICAL_REVIEW_20260919.md，必要时仅固定小数组/精确算术。
无模型、媒体、服务器、GPU、参数搜索或分类器训练。科学/预算及新候选准入门槛不变。
