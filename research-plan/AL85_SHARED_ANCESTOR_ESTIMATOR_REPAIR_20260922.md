# AL85：恢复可用的共享祖先配对点估计器

执行者 planagent，独立路径 `fallback-runtime/al85_estimator_20260922`。研究窗口 provider 启动失败回执不变，不派发或创建其他任务。AL81/AL83 的失败保持。输入定义来自 AL37/AL42；这次修复直接补齐后续评估所缺的软件原语，不作新的统计学或算法创新主张。

继承 AL83 的共享负类、祖先整数权重、固定生成器/score-tag 全集、逐 cell 原分子分母、O(n log n) 排序、Fraction 逐对参考及严格非法输入拒绝。全部输入和预期放入 config；不沿用失败 runner 生成的分数、权重或对象哨兵。标签仅为诊断标签，不称实际训练种子。

固定六个样本 p0/p1/n2/n3/p4/n5，祖先分别 A/C/A/D/B/B；g1 正类 p0/p1，g2 正类 p4，三个负类均共享。tag-a 候选分数为 4/3/1/3/5/2，基线为 3/2/2/4/4/2；tag-b 两方法均为 2/2/1/2/4/3。全1权重时 tag-a 差7/24、tag-b差0、均值7/48。A加倍时 tag-a 候选47/48、基线3/4、差11/48，均值11/96。该值由这里明确的祖先映射推导；AL83未绑定完整fixture的1/8预期不可继续借用。

仅运行一次冻结批次：四种权重（全1、A加倍、B为0、全0）、全 tie/正序/逆序/同祖先tie解析小例、记录排列不变性及单tag分数翻转。非法输入按配置中的确切错误码核对；所有检查共同决定 pass，不以异常数量替代正确拒绝。输出每个 cell 的 two_U、2WpWn、AUROC 与失效原因，完整宏平均及配对差；任一组缺类则总体 null，保留其余 cell 但不做 valid-only macro。

先进行静态审查与编译，保存源码/完整配置/参考/旧证据哈希，提交冻结版本后执行唯一 run_v1。时间由运行时读取；记录 wall/process CPU、源码/输入/输出、预期与实际检查清单。异常也落盘，存在输出目录即拒绝重跑。失败后保持 fail，另行审查原因，不在本包反复调样本或容差。

标准库单线程，无模型、媒体、SSH、GPU、网络、优化器、随机模拟、bootstrap或CI。自测不构成独立科研复核。与 AL86 的四调用身份审查彼此独立；后续保留 AL36/AL68 原包和一个由审查结果形成的可执行软件接续。
