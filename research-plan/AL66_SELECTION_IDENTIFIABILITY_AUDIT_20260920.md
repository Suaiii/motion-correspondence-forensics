# AL66：未知筛选概率下，条件源类排序能否辨识

planagent本职数学审查，独立于AL36。输入AL64的真实筛选管线描述、AL65已冻结五情形结果、AL13的同D同k与来源目标。AL65仅证明若干已知选择概率的有限表；本包处理尚未得到的未知选择机制问题，不重复其输出。

固定对象：源类Y、可见nuisance Z、候选特征Q、是否进入样本池S。分别写下源总体p(Q,Z|Y)、入选概率a_y(Q,Z)和观测分布p(Q,Z|Y,S=1)的关系。在正概率共同支持内，给出条件源类似然比何时只被Z有关常数改变、何时Q依赖的入选比可以任意改变排序；零支持单列。不得把任意匹配Z当条件独立。

允许的最小输出是一个清楚的可辨识/不可辨识命题与所缺信息，不要求硬凑算法。可考虑用入选比的预定界给排序保持的充分条件，但必须明确界从何而来；实际没有界则unknown，不能估造范围、拟合或宣称已“去偏”。标准条件概率、重要性加权和敏感性界属于经典内容，不能当新贡献或源类因果效应。

边界：只用已归档符号对象进行人工推导；不运行作者代码、数值toy、旧实验、模型、媒体、服务器/GPU或分类器。无需增加公开网页读取。本包不解决所有域迁移，只裁决“已有条件分离能否支撑来源机制”这一局部假设。若仅重述AL65，则not_applicable去重结项。

交付`reviews/AL66_SELECTION_IDENTIFIABILITY_REVIEW_20260920.md`与紧凑人工推导/输入哈希记录。结束后明确是否仅经典归因约束、是否还有足以另立新候选的算法差异；不能凭审查通过放行OR1真实训练。保留AL36不同执行者的独立准入审查。
