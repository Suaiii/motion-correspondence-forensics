# R01 开发资产复核记录（部分完成）

2026-09-07。状态仍为 running，不代表后台训练程序运行。

已完成：RoboVid清单原视频前缀交叉检查；E盘运行环境确认；120条独立前缀开发样本两时点解码、字节哈希和平均哈希检查；一对平均哈希误匹配的图像复核；元信息逻辑回归诊断；抽帧重复索引与直轨迹null检查；拒绝重复补帧的新采样计划及6项单元测试。

元信息在24条开发audit上可完全分开标签，支持优先处理帧率、分辨率和时长混杂。120条抽检均可解码两个时点，但不保证全流有效。S019抽检近黑且不足2秒；S038含动画片头。这两条在原始元信息诊断保留，尚未构成正式剔除清单。

未完成：原始源元数据映射、fake prompt与派生父视频映射、全面近重复检查、逐项数据许可/使用范围复核、内容QC规则、独立真实来源与未见生成器覆盖。前缀和字节哈希不能替代这些检查。

目前仅对已有用户数据做本地开发诊断，未发布/重新分发数据，未下载新数据。已知文件存在不等于完成许可认证。仍不能发布v03或声称M0/M1全部完成。

证据入口：`E:\aNB\TECH\脉冲神经网络\research-runs\m0_metadata_v2\research_checkpoint.md`；哈希索引：同目录 `artifact_hashes.json`。环境：`E:\aNB\TECH\脉冲神经网络\research-runtime\environment.json`。
