# AL41：撤回 AL40 错误频响判定并独立核对

负责人 planagent，限数学/证据审查。AL40 的执行记录保留，原始 pass 不作为输入真值。冻结本工作单后再核验，只处理 AL40 已存的 stride=4、omega=pi、alias=3pi/2 和核 (1,1,1)、(1,0,-1)，不替换频率/核，也不扫描补出有利反例。

采用工作单对应的 H(omega)=sum_{r=0}^2 a_r exp(i*r*omega)，用整数二元组 (real,imag) 表示高斯整数，从四次单位根实际计算每次幂和乘积；禁止只检查旧 JSON 的 passed 字段。独立写出闭式 H111=1+z+z²、H10m1=1-z² 并对照。

验证 z1^4=z2^4=1 的纯采样恒等式；记录每个核两项复响应及功率 |H|²，区别复数输出和仅功率读出。零响应的相位保持 undefined，不做除零。结果若与原回执冲突，逐项记录原值、计算值和推翻的解释。

原 AL40 文件、AL38、研究端源码和所有已暴露数据只读。新回执写 research-plan/reviews/AL41_*，保存输入、脚本、旧回执 SHA256 与真实开始/完成时间。原 AL40 改为 done/fail；若核验更复杂或不支持预期，照实保留，不能自动恢复 pass。新任务仅在预定四个核频率求值自洽时验收为纠错范围通过，不是独立科学复核或候选创新准入。

同时只读核对 AL39 报告中的检查数量与 JSON 分类数量，若不符记录为报告勘误，不重跑其已验证数值。

不访问模型/媒体/权重/服务器/GPU，不拟合模型，不创建任务会话或改身份。
