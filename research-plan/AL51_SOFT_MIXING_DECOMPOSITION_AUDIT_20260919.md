# AL51：软混合响应中的原次序项与插值曲率项

planagent，独立于AL36反方准入及AL45方法学来源恢复。AL49原数值执行合同失败保留，仅将其可复核反例作为问题线索，不再运行该例。输入AL12、AL19、AL43与AL49纠错材料；只做符号机制设计，不新增人工样本或模型调用。

待证问题：OR1的有限h响应是否完全反映原完整重建映射的次序，还是同时包含插值弦偏离？若后者存在，应在拟议真实批次中分别识别这两个来源，不能把所有非零K都命名为原解码次序效应。

固定a=1-h、0<h<=1、P_j(u)=a*u+h*R_j(u)。定义原次序差C(u)=R_a(R_b(u))-R_b(R_a(u))，以及

    J_a(u,h)=R_a(P_b(u))-a*R_a(u)-h*R_a(R_b(u)),
    J_b(u,h)=R_b(P_a(u))-a*R_b(u)-h*R_b(R_a(u)).

从原端点公式逐项展开，核查 K_h(u)=C(u)+(J_a-J_b)/h 是否成立，注明映射共同输入/输出空间及完整状态确定性条件。若等式错误照实保留，不改变定义以使其成立。

在沿弦二阶导数有上界的条件下，推导J/h的上界和h→0解释，避免把J本身小与J/h小混淆。区分线性/仿射映射、非线性、裁剪/缓存导致前提失效。判断原R可交换为何仍可有非零K，以及非零K能排除和不能排除的替代解释。

提出至多一个未来未执行的对照分解：共享原四调用，再需哪些额外端点才能计算C与J，明确每样本总调用/误差界/支持；不称同四调用，也不立即增加实际模型负载。归档原OR1不改h、eta、probe或读出；必要时另立新候选/准入，不把本代数恒等当算法创新。

交付 AL51_SOFT_MIXING_DECOMPOSITION_REVIEW_20260919.md：完整推导、成立条件、原次序与插值曲率的可证伪机制问题、成本与最小新增真实信息。无旧toy回放、无真实标签/训练/模型/权重/媒体/服务器/GPU；不足前提标unknown。
