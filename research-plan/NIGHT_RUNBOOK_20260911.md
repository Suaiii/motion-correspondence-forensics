> 本夜已于北京时间2026-09-11 03:56:07收尾并关闭35529实例；自动任务已PAUSED。下方运行状态为历史记录。最终报告：`research-runs/overnight_20260911/final_report.md`。
> 下次获准运行时，先部署 `research-runs/cvpr27_deployment/deadline-guard-fix-20260911.zip` 中的修复，并使用新的run-dir；本夜目录保留STOP_NEW_JOBS和stop_early.request，不能直接复用启动器。
# 9月10日晚至9月11日10:00执行说明

截止固定为北京时间 **2026-09-11 10:00**，不能因跨过午夜而顺延一天。用户此前允许阶段完成且无合格后续任务时提前关机。

## 已运行的实例与保护

- 当前为用户新克隆的实例：`root@connect.westb.seetacloud.com:35529`。旧23129实例保持关闭，不要操作它。
- 新实例实际16核、80 GiB内存、4090 D 24GB，数据盘只有50GB。两份归档和原运行环境已克隆保留。
- 认证仅使用当前任务中用户提供的该端口凭据；不写入文件、日志或自动任务提示。
- 项目根：`/root/autodl-tmp/cvpr27`；解释器：`environment/venv/bin/python`；运行时：`releases/v0.1.3/cvpr27-server`。
- 夜间根：`runs/overnight_20260911`。实际guard PID以`guard_status.json`和`/proc`校验为准，不能仅信本机状态文件。
- 09:40设置停止新增任务标记；09:50终止已登记的任务组并收尾；09:59调用AutoDL关机命令，预留一分钟至10:00。
- 守护程序不依赖聊天连接，日志写失败不会阻止截止分支。旧版源也保留在夜间根，新的保护程序已经实际启动并核验。
- 每个后台作业必须使用独立进程组，并在夜间根保存`job_*.json`，包含PID、`/proc/<pid>/stat`的start_ticks及命令；命令包含本夜根目录，供守护程序精确识别。

自动跟进ID：`cvpr-9-11-10`，每15分钟，截止后停止。需要本机开着且Codex运行才能处理本地文件与主动同步；服务器端截止保护独立运行。[官方调度说明](https://learn.chatgpt.com/docs/automations?surface=app)

## 已完成的新数据实验

| 实验 | 数据 | 实际结果 |
|---|---|---|
| 原8 fps、两秒协议 | ModelScope/VideoCrafter2各500条 | MS全部通过；VC2全部因不足两秒被排除 |
| 原生16帧补充诊断 | 相同1000条，不做时间重采样 | 两来源各500条均通过 |
| 无重编码变速对照 | 同队列确定性选择，每来源50条 | 100条解码帧序列全部相同，两秒准入结果100条全部翻转 |

原视频均为16帧；MS为8 fps、2秒，VC2为10 fps、1.6秒。变速会改变播放时间与时间戳，不能称为“所有因素都不变”。结果说明固定时长准入会改变来源覆盖，不证明时间信息无用，也不证明来源偏差已经消除。

后两项是受到首轮发现启发的补充诊断，不是预注册主比较。上述数据只有两个生成来源，没有真假分类准确率、AUROC或新机制训练结果。

本机首次备份：`research-runs/overnight_20260911/night_checkpoint_v1.zip`，解包目录`checkpoint_v1/`。原始主检查、原生帧补充、变速补充及源代码均已备份。

## 当前执行中的全量资产检查

`full_native_qc`正在按作者路径清单检查两个原始Pair1归档的全部 **27002条**（每来源13501）。采用独立的原生16帧诊断与12进程，保留缺失成员和解码失败，不替补样本。

该任务还保存解码帧SHA256及三个代表帧的pHash，用于后续重复候选检查；pHash接近不能自动认定同源或把祖先状态改成verified。只处理已下载原归档，尚未应用发布目录中的修复包，不能宣称已经验收整个最新数据版本。

查看`full_native_qc/qc.log`、`qc_summary.json`及`records/`，先核查对应`job_fullqc.json`的PID/start_ticks是否仍有效。不要重复启动正在运行或已完成的作业。若中断，保留现有记录与提取回执，沿原配置续跑。

## 后续可推进工作

1. 完成全量记录的质量分布、原始字节/解码重复及pHash候选统计。
2. 核对官方修复包与原始归档的关系、来源/提示映射，记录未确认事项。
3. 根据覆盖率提出新的采样协议，并保留固定物理时间与原生帧预算的区别；不能直接把补充实验改名为预注册主结果。
4. 新真实来源和完整合格清单尚未到位，正式18k机制训练的门槛仍未满足。历史100条性能样本不能填补正式训练集。
5. HD-VG公开分卷合计约80GB，当前50GB数据盘放不下完整包；不要启动无空间容纳的下载。用户已收到扩容请求，若无回复则不重复催问，不擅自租新机器。

## 截止收尾

09:40以后唤醒时直接进入收尾流程，不再等下一次15分钟调度。先处理正在完成的记录与小型结果同步；必要时以不超过60秒的单次等待保持本轮直到关机核查完成。

只备份JSON/JSONL、日志、配置、代码及必要检查点，不复制全体视频。服务器结果在数据盘保留；备份到本机后，可写入夜间根`stop_early.request`请求提前关机。没有完成备份时不要随意提前关机。

不得释放实例或删除原始归档以停止存储费用。记录关机命令、时间与复连结果；平台账单未查看时不能填写实付金额。到10:00后不得重启或启动新实验。完成最终报告后暂停本自动任务。

## GPU并行工作（用户要求提高GPU使用，01:03起）

- 已注册 `job_gpu_queue.json`，初始PID 50780；进程及子进程受同一服务器截止保护管理。
- 队列先跑固定1000条的RAFT-small与Farneback首对帧比较，同时下载冻结DINOv2权重；随后提取27002条原生16帧的DINOv2 ViT-B/14特征。
- RAFT首对帧仅用于估计器敏感性筛查，不等同完整时序机制验证。DINO特征为冻结表征缓存，不训练分类器、不选择阈值。
- GPU任务使用4个解码进程，与现有12路CPU质检并行。DINO每批8视频/128帧，bfloat16推理，每视频保存16×768特征、时间戳、输入和输出哈希；失败记录保留。
- 检查 `gpu_queue.log`、`gpu_raft.log`、`gpu_dino.log`、`gpu_raft_v1/` 和 `gpu_dino_v1/`。下载模型不算GPU运行，必须核验nvidia-smi与实际产物。
- 只使用官方公开模型地址；DINO代码固定为7764ea0f912e53c92e82eb78a2a1631e92725fc8。下载回执中的完整SHA256是本次观测值，RAFT另核对作者文件名中的哈希前缀。
- 预算沿用当前整机夜间预留，不将并行CPU/GPU作业重复计成两份整机费用。截止时间仍为9月11日10:00。

GPU队列补充：首轮RAFT的1000条均完成，耗时88.26秒；它是小模型首帧对诊断，不能承诺持续高占用。DINO单流下载速度不足，已保留原日志/部分文件并停止仅该下载子进程，改为 `job_dino_ranges.json`（初始PID 54439）管理的8路官方HTTP分段下载；校验Content-Range、ETag和分段哈希，完成后自动启动DINO。原队列中的DINO取消错误属于调度替换记录，先看新任务日志 `dino_ranges.log`，不得重复启动。新增脚本为 `dino_range_launch.py`。

DINO启动验收：分段下载已完成，CUDA特征提取实际运行，首批16个缓存的SHA256、16×768形状与有限数值检查通过。约105秒已产出1520条。`nvidia-smi dmon`的首轮5次SM采样为79、36、80、0、0%，表明计算与数据供给存在间歇，不能把80%峰值称为持续利用率。后续优先检查实际吞吐及CPU质检完成后供给是否改善，再决定有依据的流水线调整。

利用率后续：另一次10秒采样均值仅0.6%，说明不能用前一次80%峰值代表持续GPU占用。DINO记录继续增长到2640条，尚未完成流水线优化；已将本项目CPU质检进程组nice调整为10，为GPU数据供给争用CPU时让出优先级。后续需在质检完成后重新测量，按实际吞吐评估是否增加解码并发，不能通过重复无用计算制造占用率。

## 全量QC已完成并备份（01:28后）

27002条检查完成：MS13499通过、2条黑帧剔除；VC2全部13501通过。两条黑帧记录文件哈希相同。其余记录无完全相同的16帧采样序列及三帧pHash元组；未做近邻距离搜索，不视为完成祖先去重。每个来源的帧率/编码/分辨率完全一致，来源混杂仍然存在。

本机报告 `research-runs/overnight_20260911/full_qc_report.md`；完整备份 `full_qc_checkpoint_v1.zip` 已逐项验证27006个冻结输入哈希。首次复制终端折行显示时SHA字符串多出一位导致本机校验拒绝；随后重新按两段读取服务器SHA，确认本地备份一致，修正报告并通过全量校验，未修改备份或数据。DINO仍运行。CPU质检结束后的另一次10秒GPU SM采样均值14.1%，仅限采样窗口，不能当整段平均或优化因果结论。

## DINO完成与真实来源按成员读取（01:43后）

DINO共27002条完成，27000成功、2条为已知黑帧错误，耗时1902.29秒。`dino_cache_audit_v2`已验证全部27000份特征的SHA、维度、数值及与QC一致的采样时间戳。GPU近邻检索每条取前5个，cosine≥0.98的候选为0；仅为内容检索，不证明无语义重复或祖先独立。v1核验误用了记录文件命名哈希，报FileNotFoundError；v2改用sample_id映射，原失败日志保留，缓存数据未改。

Vript RAR5快速索引探针成功：只传输1760030字节，读出20126个非solid成员；作者标签有20131条，5条未在快速索引中出现，不能仅据此判为归档缺失。文件名前缀候选原视频组共1746，已冻结每组一个片段的1746条名单（`vript_selection_v1.json`），候选组不是已验证祖先。

`download_rar_members.py`将原位置头部与快速索引逐字节比较，拒绝solid/加密/跨卷/重定向成员，用最小RAR容器交给UnRAR解压，验证大小、CRC32及SHA256，保留原视频和压缩成员。单成员试验位于`vript_member_smoke_v1`，注册`job_vript_smoke.json`、初始PID92228，日志`vript_smoke.log`。成功并通过解码后可推进冻结名单前100条的下载/解码pilot；不要直接把未验证的真实来源放进正式训练。仍保留截止保护，原23129实例保持关闭。

Vript单成员试验成功：原始头与快速索引一致、UnRAR解压与CRC/大小校验通过，严格两秒和原生16帧解码均通过。已启动 `vript_member_pilot100_v1`，固定名单前100条、4路并发，注册 `job_vript_pilot100.json`，日志 `vript_pilot100.log`。查看pilot完整质量/下载结果后，再决定扩展到全部1746个候选原视频组代表；同一解压校验失败不能跳过或替补。没有整包下载或全归档SHA验收，原视频归属和许可细项仍需核对，不能因此释放正式训练门槛。

## 第二真实来源分卷头部检查（01:58后）

已通过精确Range读取并校验HD-VG分卷001的32字节起始头及分卷004末尾106字节next-header，两处CRC均正确，未下载完整80GB归档。next-header的全局偏移为80019157930，内容含UTF-16文件名 `hd_vg_130m.rar`，提示外层7z封装了RAR，需要解析库确认外层条目/压缩块后才能判断能否随机取视频，不能直接把分卷视为可按视频寻址。

原始头部与回执在服务器 `hdvg_header_probe_v1/`。py7zr当前未安装；只读查询PyPI得到1.1.3，可在独立工具目录安装并固定依赖后解析这些头部，不改现有GPU训练环境，不下载整包。Vript100条pilot仍是当前数据作业。

## Vript1746组代表正式获取（02:13后）

100条pilot全部下载和CRC校验成功，严格两秒98条通过、原生16帧99条通过；两条严格协议排除均为short_video，其中一条原生帧数也不足16，无数据损坏。已启动 `vript_member_group1746_v1`、`job_vript_group1746.json`，初始PID93820，4路并发，日志 `vript_group1746.log`。前100条逐个复核SHA后以硬链接复用原视频/压缩成员、复制记录，未重复下载。全部1746条原始与压缩成员合计预计8968498251字节，启动前余量大于24GiB。这里只是资产获取，不是通过正式训练验收。

HD-VG外层元数据库核验任务为 `job_hdvg_outer.json`、初始PID94191，日志 `hdvg_outer.log`。脚本 `inspect_hdvg_outer.py` 在单独目录安装py7zr1.1.3并记录依赖，只允许读取已经CRC校验过的138字节头部，任何payload读取会直接拒绝；不更改GPU运行环境。

HD-VG任务启动修正：首次Popen早于SCP完成，报脚本不存在，未安装或解析；旧日志保留。等待SCP退出并逐字节SHA核对远端脚本后，已注册 `job_hdvg_outer_v2.json`（初始PID94703），查看 `hdvg_outer_v2.log`。后续部署必须等待SCP完成并验收文件，不能在认证后首次yield即启动依赖任务。

HD-VG外层解析已完成：只有一个 `hd_vg_130m.rar` 成员、一个LZMA2压缩块，packed=80019157898字节，unpacked=80030858235字节；解析未读payload。结果 `hdvg_header_probe_v1/outer_archive_info.json`。不能直接沿用Vript的单成员Range获取方式。下一步应查官方替代发布/原视频访问，或先做有字节上限的流式可行性测量，不默认下载80GB压缩流，不把138字节头部验证当视频验收。

## HD-VG官方替代入口（02:28后）

已在本机读取官方Google Drive目录、README及分割脚本源码；服务器目录请求返回503。本机记录为 `research-runs/overnight_20260911/hdvg_access_notes.md`，含官方链接、文件ID和处理链观察。实际源码保存为 `.txt`，没有执行。Google下载中间页另存HTML，不能当源码使用。

官方提供YouTube URL与切片元数据，metafiles首分片hdvg_0.json约1.65GB。下一步先测试有大小上限的metadata Range读取、映射video_id/span/scene_cut/clip_id，再测固定小样本公开视频可用性；不要在服务器直接整包下载metadata或80GB视频流，也不要执行原脚本。原脚本部分片段使用mp4v重新编码，部分直接copy，必须区分处理链/发布版本。源许可和归档发布历史仍待核对，不能将这些HTTP读取称为第二真实来源已验收。

## 官方元数据分段与原视频ID交叉（02:43后）

`probe_hdvg_metadata.py`在本机仅读取官方hdvg_0.json的1MiB，HTTP206与Content-Range正确（总大小1768607448字节），解析出40个完整原视频记录；这只是字典前缀，不能称随机样本。产物 `research-runs/overnight_20260911/hdvg_metadata_prefix_v1/`。

`audit_real_source_ids.py`从两份锁定标签清单重算：Vript1746候选原视频ID，HD-VG13416候选ID，两者交集135。记录为 `research-plan/artifacts/cvpr27/real_source_id_candidates_20260911.json`，含输入/代码SHA和交集成员。今后跨集合统一分组，独立真实来源验证应排除共享候选ID；仍不能将文件名候选直接升级为verified ancestry。

已启动 `job_hdvg_availability.json`，入口 `check_public_video_availability.py`，输出 `hdvg_availability_v1/`、日志 `hdvg_availability.log`。仅测试前缀记录中固定三个字典序ID的公开元信息可用性；yt-dlp2026.8.19放在独立tools目录，禁用配置和缓存、不下载媒体、不使用cookies或登录；遇到认证/不可用保留结果，不换ID或绕过访问限制。包上传完成且SHA验收后才启动。

## HD-VG网络探针与Vript GPU流水线（02:58后）

固定3个公开视频元信息请求均失败，原因是服务器TLS握手超时；无法据此判断视频是否删除或私有。结果保存在 `hdvg_availability_v1/results.json`。暂不重复相同网络探针、不使用cookies或其他凭据。

新增 `extract_vript_dino.py`：等整批获取摘要完成后，使用冻结DINOv2-B、原生16帧、每视频QC时间戳一致性校验。预加载RGB张量并固定内存传输，ffprobe与OpenCV均单线程，默认12个数据工人、每批16视频/256帧；记录CUDA事件前向时间，避免仅用瞬时nvidia-smi占用率推断效率。首次对16帧比较FP32与bfloat16的余弦相似度，最低须>0.999；这是数值检查，不是检测效果门槛。

先启动 `job_vript_dino_smoke.json`、初始PID105914，输出 `vript_dino_smoke_v1/`，固定已完成pilot名单前16条、2工人。检查summary和precision_check后才能启动全1746条的等待/提取任务。原样本和脚本哈希验收后已部署，正式机制训练仍未启动。

Vript GPU小试通过：16/16成功，FP32与bfloat16余弦最小0.9996866、均值0.9997690。墙钟40.09秒包含数据工人启动、输入等待和数值检查，计时从模型构建之后开始，不含模型加载或此前下载；CUDA常规前向事件0.152秒，不能把墙钟都记为GPU计算时间。已启动 `job_vript_dino_group1746.json`，12工人、每批16视频，等待 `vript_member_group1746_v1/summary.json` 完成后自动提取；等待期间不加载GPU模型。查看 `vript_dino_group1746_v1/watch_status.json`、`progress.json`、`summary.json` 和 `vript_dino_group1746.log`。16条小试保留为独立实现检查，正式1746队列配置另锁定。

## 最终收尾

Vript全部1746条取得，严格两秒1703条通过（37短视频、6黑帧排除），原生16帧1727条通过并完成DINO全量特征复核。最后备份 `real_and_closeout_checkpoint_v1.zip` 的5465个文件全部逐项哈希一致，原视频和压缩成员留在服务器，未释放实例或删除盘。

03:54提前关机请求触发保护进程，但/usr/bin/shutdown为无shebang的可执行shell文本，直接subprocess执行报errno8。03:56:07改用/bin/bash调用后返回0，SSH被远端关闭，复连拒绝。调用失败和纠正都写入本机 `shutdown_verification.json`；不能声称最初的直接exec已成功。

本地deadline_guard已加入仅ENOEXEC时的Bash兼容回退，并保存errno，6项测试通过。新补丁尚未上传到已关机实例，部署包和验证记录已准备。修复代码与本夜服务器旧代码分别保留，不篡改既有快照。

N11=done；D01=blocked，原因是第二独立真实来源、许可/祖先映射、修复包与新采样协议尚未验收。正式机制训练、检测器拟合和最终确认均未运行。旧预算预留继续等待平台账单核对，不写成实付。没有新的用户请求不重启实例。
