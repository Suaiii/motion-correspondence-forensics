# AL37：祖先相关性、五种子与 macro 差的统计对象审查

planagent，独立于 AL36。输入 AL21 覆盖率边界、AL23 确定性优化限制、AL35 协议草案；仅数学/统计设计，无真实样本、模型、拟合或服务器。

固定比较两方法、G 个生成器、5 个预定种子、共享真实祖先与可能多视图；主对象候选为先每种子求 generator-macro AUROC，再求方法配对差并对五种子取均值。推导与 pooled-generator AUROC、集成分数 AUROC 的区别，不能把这些混用。

设计祖先成簇的同一重抽计划：方法/种子/共享真实祖先保持配对，跨生成器同祖先共同移动；缺某类重抽的处理规则事先声明。区分“条件于已训练五个模型的样本区间”与“随机训练过程的不确定性”，不将固定五个数重抽自动称为全部不确定性。

给出可复核公式与最多一个事前写明的最小排序反例，不进行随机模拟或功效扫参；先冻结例子，再计算。确定性凸头的五种子证据不能凭机械重复凑齐，需说明哪项真实随机变化尚待具体协议确定。

验收是统计对象/假设/重抽/缺失规则及未解决设计选择清楚，不改 +0.03/95%/4-of-5 门槛，不编造样本量或 CI，不访问最终集。交付紧凑数学审查与待绑定字段，不能记作正式统计协议已冻结。
