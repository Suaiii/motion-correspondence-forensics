# AL50：配对图与最小费用匹配在描述误差下的稳定条件

planagent，独立于AL36/AL45。输入AL12的RMS caliper=0.5最大基数/最小费用配对、AL23的平局敏感性、AL46的局部匹配方向。只作符号界和一个预注册判定合同，不新增旧数据测试或模型调用。

问题：描述b或它的固定标准化存在数值/估计误差时，配对图会不会改变，从而把配对选择变化误判成K/q的局部增量？AL46的连续Jacobian条件并不认证实际离散匹配稳定。

固定训练标准化与ID tie-break、caliper c=0.5。对每个已标准化描述给RMS误差界epsilon_i，推导距离与平方费用的扰动界。写出全部边距阈值的严格间隔如何保证caliper图不变。若标准差/均值也要重估，不能直接沿用该界，须列其传播缺口。

在图和最大基数m固定时，以最优匹配与次优同基数匹配的费用间隔Delta、每边费用误差界eta_e，推导唯一最优匹配身份保持的充分条件。区别精确平局、字典序确定性和对扰动鲁棒性；Delta=0时不能用固定ID宣称稳定。若只保留最小费用值而没保存匹配身份/候选图/间隔，该证书不可重核。

输出 AL50_MATCHING_PERTURBATION_MARGIN_REVIEW_20260919.md，明确条件是符号充分界，实际误差/间隔未知，不进行全匹配枚举、重新拟合标准化、配对重选或真实训练。失败不扩大caliper、不重排ID；必须区分固定配对下的读出机制与重新配对后的选择效应。无媒体/服务器/GPU，无新算法或独立科学复核主张。
