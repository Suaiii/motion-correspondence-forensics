# AL28：OR1时间聚合与静态对照的数学审查

待执行planagent，独立于AL27。输入AL12、AL17、AL20及AL25工作单的共同支持读出定义；不依赖AL25实验成功，不运行真实probe。

问题：时间顺序敏感性来自何处，ratio-of-means隐藏了什么权重，哪些静态对照真的保留了输入信息和计算预算？

限定工作：

1. 对所有时槽复用同一个无跨帧状态的逐帧R_a/R_b，证明时间置换与四调用路径可交换，最终空间滤波/时间mean读出不含顺序信息。对实际3D/因果cache探针只指出该前提可能失效，不直接推断已经存在有效时间机制。
2. 写清mean(N)/(mean(D_raw)+eta)与逐帧q的关系：当支持相等时它是以D_raw,t+eta为权重的逐帧比值平均，一般不是无权平均。固定两帧抽象n=(1,0)、d=(1,9)、eta=1e-12作精确说明，不赋来源标签或运行模型。
3. 给静态对照的输入/成本表：单帧复制虽然四调用但少看帧；将每个原帧分别复制成完整序列并汇总保留全部内容，却需要每帧四次调用。不能将二者各自的优势混成“同信息同成本”。明确input shuffle重算与仅shuffle已有输出的区别，不提出静态响应必须为零的错误验收。

输出reviews/AL28_TEMPORAL_READOUT_REVIEW_20260919.md及必要精确表。通过仅 `temporal_and_static_control_contract`。不改已冻结h/eta/聚合，不碰真实PTS、媒体、模型、服务器或GPU；不声称新时间算法或泛化收益。
